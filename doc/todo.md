# 今後の課題

実機と突き合わせて「合っている」ところまでは来た（発音指示 2851 件一致、振幅
88.7% が 10% 以内）。ここから先に残っているものを、**直す順**に並べてある。

順番の理由は 3 つ。(1) 直したことで音が変わったと気づける仕組みが先に要る。
(2) 遅れの半分は音源ではなく外の配管なので、安い手から取る。(3) 速さの話は
待ち時間の下限を決めるので、詰めるなら最後にまとめて。

---

## やる順

### 1. `make test` と基準の波形をこのリポジトリに置く — 済（doc/testing.md）

いま回帰試験が**別のリポジトリに散っている**。基準の WAV と MAME 側の改造が
`MU2000` 側にあり、`statetest` と `verify` はあるが Makefile に試験の的が無い。

この状態では、以下のどれを直しても「音が変わったことに気づけない」。
design.md が移植で一番怖いと書いた事態そのもの。

やったこと（くわしくは doc/testing.md）:

* `make test` で verify → statetest → 鳴らし比べ 7 件 → スレーブ別糸の確認まで。
  ROM が無い機械では verify だけが走る。ひと回し 40 秒
* **WAV は置かない。**1 件 4MB になるうえ、ROM から起こした音を配ることに
  なるので、同梱しない立場と合わない。代わりに音から測った数（PCM の
  ハッシュ・keyon・包絡線・直流）を `tests/*.json` に置いた。全部で 40KB
* 鳴らす MIDI も置かず `tools/make_test_midi.py` が毎回組む

途中で**出る音が実行ごとに変わる**のが見つかったので直した。デバイスの状態に
初期値の無い要素があり、firmware がそれを読む経路があったため、起動時の引数の
文字列がそこに残っていると音が変わっていた（パスの区切りで keyon が 1 サンプル
ずれる）。`swp30.h`・`sh_*.h`・`sci4.h`・`hd44780.h` の要素に初期値を書いた。
**MAME 上流にも同じ穴がある**はずなので、送れる。

### 2. 出力をデバイスの形式に合わせ、遅れの内訳を数字にした — 済（一部）

**先に前提を直す。** ここに「MIDI をサンプル単位にすれば平均 15ms 消える」と
書いていたのは誤り。DAW は未来のイベントを渡してくるのでサンプル単位化が効くが、
実時間の MIDI 入力は**必ず過去に届く**。ブロックの頭で流すのは「できる限り早く」
であって、遅らせてはいない。サンプル単位化で消えるのは**ブロックの中のずれ**
（連打やアルペジオの刻みが境界に丸まる）で、平均の遅れではない。

やったこと:

* **デバイスが言ってくる形式のまま開くようにした。** この機械は 48000Hz の
  float を言ってきて、44100/16bit は素では通らない。つまり今までは
  **毎サンプル Windows の変換器（既定の品質）を通っていた**。`ui::resampler`
  （VST3 にあった窓関数付き sinc を出したもの）で自前に変換する。
  正弦で測った誤差は 100Hz-5kHz で -92dB、15kHz で -86dB
* `live.exe` が独自に持っていた WASAPI の手順を `ui::audio_out` に寄せた。
  gui と二重だったものが 1 つになり、変換もどちらにも入る
* `audio_out::start` の**宙ぶらりんの参照**を直した。立ち上がりの首尾を
  start のローカルに置いてスレッドが参照で掴んでいたので、スレッドが
  run() を抜けた後にそこへ書いていた（最適化を上げると落ちる）
* デバイスとの折衝の結果を表に出した（`48000 Hz 2 ch float / 溜め 30.0 ms /
  周期 10.0 ms / 変換 自前 sinc`）

**溜めをどこまで詰められるかを測った**（`dense` = 16 パート同時の重い側）。

| ブロック | ぶん | 平均 | 99% | 最悪 | 超過 |
|---|---|---|---|---|---|
| 1323 | 30.0ms | 16.9 | 21.3 | 21.7 | 0/500 |
| 480 | 10.9ms | 6.3 | 8.2 | **9.2** | 0/1379 |
| 256 | 5.8ms | 3.3 | 4.6 | 5.2 | 0/2584 |
| 128 | 2.9ms | 1.7 | 2.4 | 3.7 | **2**/5168 |

音源は実時間の 58% を食う。周期 10ms では最悪 9.2ms でぎりぎり間に合い、
溜め 30ms なら 20ms の余裕が残る。**溜めを 20ms にすると余裕が 10ms になって
最悪値とほぼ並ぶ**ので、いま詰めると時々切れる。README が「待ち時間の下限は
音源の速さで決まる」と書いているのはこのこと。数字が出た。

この機械では `IAudioClient3` の共有モードの最小周期も 10ms（既定と同じ）で、
そちらから取れるものは無かった。

#### 追記: 溜めを満杯にしていたのを直した

実機と並べて聞いた実測で **0.117 秒** 遅れていた（Domino → exe → 出力 と、
Domino → exe の THRU → USB → 実機 → S/PDIF の比較）。探したら作りの問題が
あった。

**起きるたびに溜めを満杯まで書いていた**（`want = buf_frames - padding`）。
満杯に保つと、新しく書いた音は常に「溜め一杯ぶん」待たされる。30ms 頼んだら
30ms そのまま遅れる。いまは `target` までしか書かない。

`--exclusive` も足した。Windows の混ぜ合わせを通らないので、そこが丸ごと消える。

| | 溜め（平均/最悪） | デバイス | 出るまで | 遅れ |
|---|---|---|---|---|
| 共有 目標 20ms（新しい既定） | 10 / 20 | 0 | 約 10-20ms | 0 回 |
| 独り占め 10ms | 0 / 10 | 10 | 約 10ms | 0 回 |
| 独り占め 4ms（音源なしで確認） | 0 / 4 | 4 | 約 4ms | — |

`GetStreamLatency` は共有モードで 0 を返す（役に立たない）。`IAudioClient3` の
共有モード最小周期もこの機械では 10ms で既定と同じだった。

#### 決着: 117ms → 16ms

実機と並べて波形で測って **16ms**（60fps で 1 フレーム）。生演奏に使える。
効いたのは 3 つで、うち 2 つはこちらの欠陥だった。

**(1) 独り占めの周期がデバイスの単位と噛み合っていなかった。** ドライバは
何を頼んでも「通った」と返してくる（3ms でも 5ms でも器はその通りに返る）が、
カードは 1024 サンプル単位で動いていた。480 フレーム（10ms）は 1024 を
割り切らないので、**切り刻まれたような音**になっていた。2 の冪に丸めて解決。

  切り分けの手順が効いた。デバイスへ渡すバイト列をそのまま WAV に落として
  調べたら、440Hz の正弦が 1 サンプルの差まで期待どおりで飛びも 0 個だった。
  「こちらのデータは正しい、崩れているのは渡した後」と分かった時点で、
  疑うところが受け渡しの作法に絞れた。

**(2) 形式。** この機械の独り占めが受けるのは 48000/16bit と 24bit in 32bit
だけで、float は全滅。**24bit in 32bit（業務用の本来の形）を先に試す**ように
した。16bit も「通る」と言うが扱いが怪しい。

**(3) インターフェース側の設定**（これはこちらから触れない）。
Buffer Size 1024 → 256、Sample Rate 48000 → 44100。後者は MU2000 が 44100
でしか動かないので、**こちらの変換が丸ごと消える**という副産物もあった。

同じ機械で並べた最後の数字:

| | まだ鳴っていない量 |
|---|---|
| 共有 | 47.5ms（こちらの溜めは 10ms。差の 37ms は Windows とドライバ） |
| 独り占め | 11.6ms（周期そのもの。余分なし） |

**出口を選べるようにした**（`--audio`、名前の一部で指定、gui.ini に記憶）。
Windows の「既定の再生デバイス」は勝手に変わり、実際この作業中に別の口へ
移った。聞いている口が決まっているなら指定したほうがよい。

`--exclusive --latency 5` なら周期 5.8ms まで行けるが、たまに間に合わない
（音源の 1 ブロックの最悪が 3.4ms、余裕 5.8ms）。**ここから先は音源を
速くする話**（6 番）。

残り:

* **曲の再生（`--play`）をサンプル単位にする。** ここは player が未来の譜面を
  持っているので、サンプル単位化が本当に効く唯一の場所。いまは別スレッドが
  実時間で輪に積んでいて、音声側がブロックの頭でまとめて飲む。音声スレッドの
  標本時計で流せば `render.exe` と同じ音になり、`make test` で画面側の経路も
  見られるようになる
* 溜めを詰めるのは 6 番（速くする）の後

### 3. 30Hz 以下の残りは MEG（エフェクト DSP）が作っている — 出どころを特定

30Hz 以下が 5.3% 残っている（実機 0.13%）。**積算器の丸めだという読みは外れで、
出どころは MEG だった。** 順に潰した記録を残す。

#### 1. ミキサの丸め（`mixer_att`、`swp30.cpp:2701`）— 違った

`>>` が負の側で −∞ へ落ちるので偏るはず、という筋で最近接と 0 方向の二通りを
試した。**直流は 0.3% も動かない。**

| | floor（いま） | 最近接 | 0 方向 |
|---|---|---|---|
| piano | 4.239 | 4.172 | 4.216 |
| chord | 24.785 | 24.689 | 24.719 |
| dense | 169.789 | 169.271 | 169.384 |

理由は後で分かった。MU2000 の使う経路は**減衰なし**のモード（`mixer_step` の
mode 1-3）で、そこでは `mixer_out[] += input` と足すだけで丸めが無い。
`mixer_att` はほとんど呼ばれていない。

#### 2. 声ごとのフィルタ（Chamberlin と iir1）— 違った

再帰積分器の中の丸めは直流に化けやすいので、`filter_block::step` と
`iir1_block::step` の `>>` を全部最近接にした。**piano 4.239 → 4.250。**
iir1 は 0.00%（この曲では通っていない）。

#### 3. 声の出口を直に測った — 声は直流を持っていない

`render --dump-dac` で 1 声の中身を 1 秒ぶん出し、段ごとの平均を取った
（piano の減衰中）。

| 段 | 平均 | 最大 |
|---|---|---|
| wave（読んだ生のサンプル） | −36.9 | 12272 |
| filt（フィルタ後） | −1150.3 | 301049 |
| out（音量をかけた後） | **−0.175** | 1470 |

声の出口はほぼ 0。**直流は声より後で入っている。**

#### 4. MEG の出口に乗っていた

同じ記録に MEG のレジスタも入っている。ミキサが DAC に入れている線
（`m_m[0x20]`, `m_m[0x21]`）の平均は **+354 / +356 で、最大が 2667**。
出力の 13% が直流。

送りを 0 にした MIDI（CC91/93/94 = 0）で裏を取った。

| | 発音中 | 減衰後（最大 93） |
|---|---|---|
| リバーブ有り（XG の既定） | +6.10 | **+4.96** |
| 送りを 0 | +2.47 | **−0.016** |

**尾の直流はまるごと MEG。** XG の初期状態はリバーブが入っているので、
全部の音に乗る。残る +2.47 は乾いた側の話で、こちらは振幅の 0.1%。

#### 5. MEG の中のどこか — 半分は `meg_pack24`

`meg_pack24`（`swp30.cpp:3226`）の `p >> 15` は、MEG が 1 サンプルに 384 歩
走るあいだ何度も通る。ここを最近接・0 方向にすると:

| | floor（いま） | 最近接 | 0 方向 |
|---|---|---|---|
| piano | 4.239 | **2.237** | 2.425 |
| chord | 24.785 | 22.278 | 22.233 |
| dense | 169.789 | 167.470 | 167.435 |
| effects | 8.149 | 6.818 | 6.016 |

**軽い曲では半分が消えるが、重い曲ではほとんど動かない。** 残りは振幅に
比例しているので、純粋な切り捨てではなく掛け算側の非対称（`m1_expand`、
飽和モード、リバーブ RAM の詰め方 18bit）が怪しい。

#### 6. 重い曲の直流の大半は、圧縮サンプルのループの読み落とし（2026-09-14）

doc/upstream.md の 12。ループを 2 つ進みで跨ぐと差分を 1 つ落とし、積算器のずれが
溜まっていた。直すと dense の直流が **172.6 → 18.6**、30Hz 以下が **17.6% → 9.6%**。
piano・chord・effects は動かない（ループを跨ぐときに 1 つずつしか進まない音）。
上の表の「重い曲ではほとんど動かない」はこれで説明がつく。軽い曲に残る分は
`meg_pack24` の側。

#### 7. 音が止んでも 0 に戻らない — 直した（2026-09-14、upstream 18）

丸めが 2 か所違っていた（p を詰めるときの切り捨ての向きと、リバーブ RAM の e = 0 の負の値の反転）。両方直すと実機と同じくぴったり 0 に戻る。以下は調べた時点の記録

実機はパフォーマンスモードで 1 音鳴らして離すと、しばらくして S/PDIF が**ぴったり 0**
（-240dB）になる。エミュは離したあと -78dBFS の雑音と直流 8.1e-4 が残り続ける
（鳴らす前は 0）。

* MEG のディザ（`rand() & 0x07e0`）を全部外しても変わらない（-78.6dBFS、直流 7.9e-4）。
  ベロシティ 40 で倍音の間の床がエミュだけ +10〜17dB 高いのも、これで変わらなかった
* `meg_pack24` を 0 方向の切り捨て（`p / 32768`）にすると**直流は 5.4e-6 に消える**が、
  雑音は -79.8dBFS で残る。最近接（`(p + 0x4000) >> 15`）は直流 1.3e-4、雑音 -79.6dBFS
* 無音の区間で 0 でないのは、プログラム頭のマスター EQ の段（r64-r77、`<< 2`・`<< 4` の
  nodither の書き込み）が ±300 前後、リバーブの r0a-r17 が ±数十。DAC の m30/m31 は ±550 で、
  出力の最大 15LSB と合う。**リミットサイクル**

**2026-09-16 の測り直し**: パフォーマンスモードの 100 パフォーマンス（実機 XG ではなく PFM、S/PDIF）を、1 件ごとに全チャンネルのオールサウンドオフで前の音の尾を断ってから選び直す形で録り直した（scratchpad の pn100b.mid・pncmp3.py）。帯の食い違いの平均は 0.765dB。実機どうしの再現性は 0.0〜1.1dB なので、これより小さい差は測れない。悪い順は 012 Old EP 3.91、067 Crypt 3.42、076 Orion 2.67、004 Analog Age 2.23、088 Tiny Pipe 2.14dB。どれも倍音そのものは合っていて、**倍音の間と上の床がエミュだけ 5〜20dB 高い**のが効いている。012 Old EP で、基音の倍音の山と倍音の間の床の比を測ると実機 96.6dB・エミュ 84.8dB（11.8dB の差）。エミュの中を段ごとに見ると（PFM_DUMP で m レジスタを 1 秒ぶん）、声が MEG に入る時点（m2c）で 89.9dB、MEG を出た所（m30）で 84.1dB。床の形は、声の段は 500Hz から 16kHz まで平らな -87dB（丸め誤差らしい）、MEG を通ると 1.5kHz が -76.5dB の山なりに変わる。MEG のディザ（rand() & 0x07e0）を外しても 84.5→84.8dB と変わらない（9/14 と同じ）。つまり残っているのは MEG の段の丸め（または限界周期）。次はここ。

**2026-09-16 の続き（丸めの候補を 3 つ潰した）**: 床の出どころを探して、次の 3 つを切り替えて測ったが、どれも効かなかった。(1) MEG のディザ（`rand() & 0x07e0`）を外す → 山と床の比は 84.5→84.8dB（変わらない。9/14 と同じ）。(2) `meg_pack24` の丸めを -∞ 側・近い方（2 通り）に変える → XG の試験（E.Piano にリバーブ HALL 1）で 91.5・91.5・91.3・91.4dB、PFM の 012 でも -∞ 側にして床は ±0.00dB。ただし -∞ 側は音を離したあと 0 に戻らなくなる（残り 8）ので、今の 0 側が正しい。(3) リバーブ RAM の 11bit 仮数を近い方へ丸める → XG の試験で 91.5→91.8dB（0.3dB だけ）。遅延メモリへ書く値の丸め（今は -∞ 側）は 0 側にしても変化なし。なお床の差そのものは、倍音 6 本で音量を合わせて測り直すと、012 Old EP で**倍音は ±1dB 以内・倍音の間の床が +2〜+9dB（平均 +4.5dB）**。最初に書いた 11.8dB は、音量合わせを帯の中央値で取っていたための過大評価。段ごとでは、声が MEG に入る所で床は 500Hz〜16kHz まで平らな -87dB（白い雑音＝丸め誤差らしい）、MEG を通ると 1.5kHz が -76.5dB の山なりに変わる（帰還のある段で形が付く）。次の候補は AWM2 の声の道（補間・フィルタ・音量の掛け算）と、MEG の段の語長。切り分けには「XG モードでエフェクトを全部切った素の音」を実機と比べるのが要る（実機のモードを XG に戻してもらう必要がある）。

**2026-09-16 の結び（床の正体は、声を重ねたときの干渉だった）**: XG モードに戻して切り分けた（scratchpad の xgfloor.mid・modfx.mid・layers.mid、floorcmp.py）。エフェクトを全部切って 1 音だけ鳴らすと、倍音の間の床は実機と **+0.16 / -4.65 / -0.02dB**（SineLead・E.Piano・Strings）で、こちらが高いということはない。リバーブを足しても -2.5〜-0.1dB。変調系のインサーション（PHASER 1・CHORUS 1・CELESTE 1・ENS DETUNE・FLANGER 1・SYMPHONIC）でも ±0.35dB に収まる。つまり声の道も MEG も、単音では実機と同じ静けさ。 ところが**同じ音色・同じ音を複数のパートに重ねる**と差が出る: 1 パート -0.06dB、2 パート +1.76dB、4 パート **+4.17dB**、4 パートでも少しずつ音程をずらすと +0.47dB。重ねた音は互いに打ち消し合って深い谷を作るので、谷の深さが声の立ち上がりの時刻の差（数サンプル）に非常に敏感になる。エミュの 4 声は MIDI のバイトの間隔どおり 42 サンプル（0.95ms）ずつずれて立ち上がる。実機も同じはずだが、upstream.md の 36 で詰めた鍵の時刻の精度は ±16 サンプルなので、その差が谷の埋まり方として出ている。 100 パフォーマンスで床の差を出すと**中央値 +0.36dB**、+3dB を超えるのは 7 件だけ（012 Old EP +7.6、076 Orion +7.1、007 Super Bass +6.7、026 Sparkle Pad +4.3、004 Analog Age +3.9、067 Crypt +3.8、074 AfricanGrand +3.1）。どれも倍音は ±1dB 以内で合っていて、層を重ねるパフォーマンスばかり。つまり残っているのは「声の立ち上がりの時刻をもっと合わせる」話で、雑音や丸めの話ではない。

**2026-09-16 夜の測り直し（物差しの誤りを直し、実機どうしの限界を出した）**: 3 つ分かった。
(1) **これまでの数字には測り方の誤りが混じっていた。** 帯を比べる前の音量合わせに「全 22 帯の中央値」を
使っていたので、信号の無い静かな帯（床）に引っ張られていた。いちばん悪いとされていた 012 Old EP は、
全帯が揃って -3.9dB ずれているだけで**中身は一致**していた（波形の相関 1.000、1.2 秒まるごと）。
音量合わせを**信号のある帯だけ**（山から 40dB 以内）で取るように直した（scratchpad の pncmp4.py）。
(2) **実機どうしの再現性は 0.216dB**（同じ pn100b.mid を 2 回録って比べた。pn100b_hw.wav / pn100b_hw2.wav）。
これがこの物差しの限界で、これより小さい差は測れない。
(3) その物差しで、今のエミュは **0.584dB**（実機 2 回のうち近いほうと比べて。pnreal.py）。
超過は 0.37dB。今日のピッチ EG の直し（upstream 13 の追記）で 0.765 → 0.719 → 0.636dB と縮んだぶんも入っている。

**本当に外れているもの**（エミュの差 − 実機どうしの差、上位）: 093 Clav-babe +1.52、086 MUtopia +1.34、
067 Crypt +1.22、004 Analog Age +1.13、018 Full Organ +1.12、054 White Train +0.98、053 Dance Chord +0.98dB。
逆に 013 Sync SynQ（1.89dB）は実機どうしでも 1.20dB あり、ほとんど測定の揺れだった。

**018 Full Organ の中身**: 倍音ごとに見ると 1〜16 倍音は ±1.7dB で合うのに、**24 倍音（6278Hz）だけ
エミュが 13.4dB 低い**（48 倍音も 7.5dB 低い）。この音は 4 声で、うち 2 声が元の録音より
+29.5 / +31.0 半音（5.4〜6 倍の速さ）で鳴っている。高い速さで鳴らす声の扱いが疑わしい。

**2026-09-16 夜、018 Full Organ を追った（3 つの形を試して、どれも違った）**: 欠けている
6262Hz は、ch05 が **169 サンプルの輪を 6.0 倍速で**鳴らしているもの（輪の周期 28.2 サンプル）の
4 倍音だった。同じ波形を ch02・ch04 が等速で鳴らしている。実機の山は −25.5dB で立っているのに、
エミュは −38.6dB で周りの高さ（−39dB）に埋もれている。**周りの高さは同じで、山だけが立たない**
＝周期性が崩れている形。

輪の折り返しで `m_pos_dec += (m_start >> 15) & 0x7e00`（この声では 0.125 サンプル）を足しているのが
怪しかったので、3 つ試した（100 パフォーマンスの物差しで。実機どうしは 0.216dB）。

| 形 | 018 | 全体 |
|---|---|---|
| いまの形（折り返しで足す。MAME のまま） | 1.33 | **0.584** |
| 足さない | **0.58** | 0.580 |
| 折り返しで位相を揃える（足すのでなく代入） | 0.88 | 0.862 |
| 速さの補正として毎サンプル効かせる | 1.27 | 0.586 |

**足さない形は 018 を直すが、ほかのオルガンを悪くする**（008 70s Organ AT 0.44→0.60、
065 Draw Organ MW 0.25→0.42、037 Early Bird 0.61→0.81）。位相を揃える形は全体が大きく悪化する
（088 Tiny Pipe 0.09→2.76）。速さの補正は 018 をほとんど直さない。つまり**周期性の崩れが原因では
なく、この声にだけ端数が掛かってはいけない**、という形に見える。条件（`m_finetune_active` の前後、
start の別のビットの意味など）を見直すのが次。いまの形が 3 つの中では一番良いので、変えずに置いた。

実機は回らないので、丸めの形がどこか違う。`<<` の前で積の下の桁を落としているか、
書き込みの丸めが段によって違うか。「離したあと 0 に戻る」は実機で白黒の付く物差しになる
（パフォーマンスモードのままで測れる）。

**2026-09-16 夜、残りの 0.37dB がどこから来るのかを潰して回った**。エミュ 0.584dB・
実機どうし 0.216dB の差について、疑わしいものを順に測って、次のことが分かった。
**どれも「原因ではない」という結果**なので、探す先はだいぶ狭まった。

*(1) 層どうしのキーオンの時刻差は、いまの形がすでに一番良い。* 093 Clav-babe の
2 声は 44 標本（1.0ms）ずれて立ち上がる。この間隔だけを N 標本ずらす仕掛けを一時的に
入れて振ってみた（全部のキーオンを 128 標本待たせ、200 標本以内に来た 2 つ目だけ 128+N に
する形）。

| ずらし | −40 | −20 | −10 | 0 | +10 | +20 | +40 | +80 |
|---|---|---|---|---|---|---|---|---|
| 帯の差 | 3.24 | 2.42 | 1.75 | **1.56** | 1.81 | 2.47 | 2.87 | 4.93 |

0 が谷で、±10 標本で +0.2dB、±40 標本で +1.3dB 悪くなる。つまり
upstream.md の 36 で詰めた鍵の時刻の精度（±16 標本）は、この音で 0.2〜0.5dB ぶんの
揺れとして効いているが、1.5dB の食い違いを説明するほどではない。試した仕掛けは戻した。

*(2) 帯ごとの「癖」は無い。* 100 パフォーマンスをまとめて帯ごとの差の平均を取ると、
100Hz から 12.8kHz までどの帯も **±0.3dB 以内**（実機どうしも同じ）。傾きも山も無く、
残っているのは 1 件ごとのばらつき（標準偏差 1.0〜1.5dB。実機どうしは 0.3〜0.5dB）だけ。

*(3) 乱数は関係ない。* 乱数の種を変えて丸ごと録り直すと、40M 標本のうち違うのは 19%、
差は**最大 15LSB（−66dBFS）**。MEG のディザと LFO のランダム波形は音を動かしていない。

*(4) 包絡線にも癖は無い。* 鳴り始めから 1.5 秒を 8 区間に切って比べると、
どの区間も中央値は ±0.2dB。ただし 1 件ごとのばらつきは大きく（032 Alien Peace は
50ms で −9.2dB、300ms で +5.7dB）、変調の掛かった音ほど大きい。

*(5) エフェクトの種類でもない。* 使っているエフェクトごとに誤差を集めても、
全体（+0.368dB）から離れるのは var:ROTARY +0.25dB（6 件）・cho:FLANGER +0.17dB（6 件）
くらいで、件数を考えると差とは言えない。

*(6) 「小さすぎて聞こえない帯」のせいでもない。* 数える帯を山から 10dB 下までに絞っても
エミュ 0.581 / 実機どうし 0.171、40dB 下まで数えても 0.584 / 0.216 で、**超過は
0.34〜0.41dB のまま変わらない**。いちばん大きい 5 帯だけを見ても同じだけずれている。

*(7) 声の道に残っている。* 034 6-string GT（声が 1 つ、8bit、80 標本の輪）で、
実機に CC91・93・94 を 0 にして録り直した（エミュでは音を離した後が完全な無音になるので、
エフェクトが切れているのは確か）。それでも **4kHz 以上が +1.5〜1.9dB、10.2kHz の帯だけ
+4.4dB** ずれる。エフェクトを通さない素の声でこうなるので、残りは AWM2 の声の道にある。

同じ音で細かく見ると、**音の高さは 0.06 セントしか違わず**（実機 522.8867Hz・
エミュ 522.9045Hz）、0.1 秒ごとの波形の相関は 0.94〜0.997。倍音そのものも倍音の間の床も、
8〜11kHz で揃って 2〜5dB 高い（そこでの絶対値は山から −45〜−65dB）。

音色の明るさ（CC74）を 24 から 127 まで振っても 10.2kHz の +4.4 / +4.2 / +3.6dB は
**ほとんど動かない**ので、フィルタの切る高さの対応表の誤りではない。鍵を上げても同じ
（鍵 72・84・96 で +5.6 / +7.3 / +6.2dB）。8bit のサンプルを使う組のほうが高い方の誤差が
大きい傾向はある（8bit を使う 46 組 +0.28dB、使わない 34 組 +0.06dB）が、弱い。

次に切り分けるなら、同じ声について ROM の波形を取り出して、補間表とフィルタを
浮動小数で自前に通したものと、エミュの出力・実機の出力の 3 つを並べる。
補間の側か固定小数の丸めの側かが、そこで分かれるはず。
（使った物差し: scratchpad の pnbias.py・pnthr.py・pnfx.py・pnenv.py・pnvoice.py、
実機の録り直しは ratescan.py・cutscan.py・drytest.py と dryres.py・drydrift.py）

**2026-09-16 夜の続き（声が 1 つの音で実機と突き合わせた。声の道は合っている）**:
声が 1 つだけのパフォーマンスを選び、音色の明るさ（CC74）を 24 から 127 まで振りながら
同じ鍵を鳴らして、実機と録り比べた（scratchpad の cutscan.py。終わりに CC74 を 64 へ戻し、
全チャンネルの音を止めている）。

**080 Bass + Piano/Brass（声 1 つ、圧縮 8bit、輪 250〜18859 標本）は、切る高さの端から端まで
0.04〜0.10dB で合う。**

| CC74 | 24 | 34 | 44 | 54 | 64 | 74 以上 |
|---|---|---|---|---|---|---|
| 切る高さ | 1167Hz | 1718Hz | 2787Hz | 4289Hz | 6663Hz | 7350Hz（k=1.00、上限） |
| 帯の差 | 0.04 | 0.05 | 0.06 | 0.06 | 0.10 | 0.10 |

実機どうしの再現性が 0.216dB なので、**これは測れる限界より細かく一致している**。
つまりサンプルの読み出し・圧縮の展開・補間・レゾナンス付きフィルタ（k が上限の 1.00 まで）・
包絡線・ミキサ・エフェクトの道は、そろって合っている。残っている食い違いは
「模型が違う」のではなく「特定の声だけ違う」形をしている。

一方 **034 6-string GT（声 1 つ）は同じ測り方で 0.4〜1.4dB ずれる**。ずれは
**8〜16kHz に固まっていて**、10.2kHz の帯だけ +4.4dB、12.8kHz 以上は −2.5〜−4dB。
リバーブ・コーラス・バリエーションの送りを 0 にしても変わらない（エミュでは音を離した後が
完全な無音になるので、確かに切れている）。音の高さは 0.06 セントしか違わず、
0.1 秒ごとの波形の相関は 0.94〜0.997。

この食い違いについて、次の 3 つを試して**どれも違った**。

* **丸め方**: フィルタ（`>>16`・`>>7`）・補間（`>>10`）・IIR1（`>>13`）を
  切り捨てから近い方へ変えても、差は 0.01dB も動かない。
* **Chamberlin の並び**: 低域の更新に「新しい帯域」ではなく「前の帯域」を使う形にすると、
  034 は 0.95〜1.38 → 3.4〜4.1dB と**大きく悪くなる**。いまの並びが正しい。
* **切る高さの倍率**: 034 だけなら k を 0.8 倍すると 1.04 → 0.55dB まで良くなり、
  必要な倍率は k が大きいほど大きい（k=0.18 で 1.00、k=0.88 で 0.78）。
  ところが **080 では端から端まで 1.000 が最良**（k=1.00 でも 0.10dB）。
  つまり切る高さの対応表は正しく、034 の話は別のところにある。

**輪（ループ）の短さ**は弱い手掛かりになる。100 パフォーマンスを一番短い輪で分けると、
100〜200 標本の組（31 件）が +0.463dB でいちばん悪く、全体の +0.34dB より高い。
悪い方の常連も 067 Crypt 128・053 Dance Chord 141・035 Phase Strings 141・
069 Guitar & Sax 145・028 Overdrive OR 150・018 Full Organ 169・040 R&B Gig 169 と
短いものが並ぶ（034 の鍵 84 の声も輪 80 標本）。ただし 004 Analog Age（32990）や
086 MUtopia（1316）のように長いものも上位にいるので、これだけでは説明が付かない。

次は 034 の鍵 84 の声（8bit、輪 80 標本、エフェクト無し、実機の録音あり）を材料に、
輪の折り返しのところだけを実機と並べて見る。018 Full Organ で行き詰まった
`m_pos_dec += (m_start >> 15) & 0x7e00` の話と同じところに戻ってくるが、
今度は**声が 1 つでエフェクトも無い**ぶん、ずっと見やすい。
（scratchpad の cut80.mid / cut80_hw.wav・cut34.mid / cut34_hw.wav・dry34.mid / dry34_hw.wav）

**2026-09-16 夜の結び（残りは「層を重ねたときの干渉」で、声の数で決まる）**:
100 パフォーマンスを**同時に鳴っている声の数**で分けたら、はっきりした
（scratchpad の pnlayers.py。声の数はキーオンのログから数えた）。

| 声の数 | 件数 | エミュ | 実機どうし | 超過 |
|---|---|---|---|---|
| 1 声 | 20 | **0.174** | 0.026 | +0.147 |
| 2 声 | 22 | 0.608 | 0.172 | +0.436 |
| 3-4 声 | 36 | 0.656 | 0.269 | +0.388 |
| 5 声以上 | 22 | 0.815 | 0.347 | +0.468 |

**声が 1 つの音では、エミュは 0.174dB しかずれない**（実機どうしは 0.026dB）。
声を重ねるほど、エミュも実機どうしも一緒に悪くなる。山から 10dB 下までに絞っても同じ形
（1 声 0.137 / 0.016、5 声以上 0.884 / 0.318）。

キーオンのばらつき（いちばん早い声と遅い声の差）で分けても同じ向きに出る。

| ばらつき | 件数 | エミュ | 実機どうし | 超過 |
|---|---|---|---|---|
| 0-1 標本（同時） | 34 | 0.344 | 0.095 | +0.249 |
| 1-100 標本 | 38 | 0.761 | 0.238 | +0.523 |
| 100-400 標本 | 14 | 0.726 | 0.273 | +0.452 |
| 400 標本以上 | 14 | 0.544 | 0.393 | +0.151 |

つまり **9/16 に XG モードで見つけた「重ねた声どうしの干渉」が、
パフォーマンスモードの残り 0.37dB のほとんど**だった。重ねた音は互いに打ち消し合って
深い谷を作るので、谷の深さが立ち上がりの時刻の差（数標本）に非常に敏感になる。
実機自身も同じ理由で再現しない（0.026 → 0.347dB）。

**次にやること**: 鍵の時刻の精度を上げる。いまは upstream.md の 36 で詰めた ±16 標本で、
093 Clav-babe で測った感度は ±10 標本で +0.2dB・±40 標本で +1.3dB。ここを数標本まで
詰められれば、残りの大半はそれで埋まる計算になる。逆に、声の道（サンプルの読み出し・
補間・フィルタ・包絡線）をこれ以上いじっても意味がない。

**声が 1 つの 20 件の内訳**（超過の大きい順。声の道だけを見るならこの表が材料）:

| 番号 | 名前 | エミュ | 実機どうし | 超過 |
|---|---|---|---|---|
| 058 | Rich Nylon Guitar | 0.63 | 0.02 | +0.61 |
| 034 | 6-string Guitar | 0.47 | 0.05 | +0.42 |
| 009 | Touch Line | 0.39 | 0.06 | +0.33 |
| 084 | Phaze Clavi | 0.38 | 0.06 | +0.32 |
| 048 | Warm Amp | 0.29 | 0.01 | +0.29 |
| 023 | Tecspiration | 0.23 | 0.04 | +0.19 |
| 080 | Bass + Piano/Brass | 0.18 | 0.00 | +0.18 |
| 052 | Old Clavi | 0.18 | 0.04 | +0.14 |

残りの 12 件（041・029・012・006・066・061・075・005・050・081・100・031）は
**超過 0.10dB 以下**で、うち 7 件は 0.02dB 以下。つまり声の道は、ふつうの音色では
実機どうしの再現性の下に入っている。上の 5 件だけが例外で、034 は 8〜16kHz の
**山から −55〜−70dB の所**の食い違い（倍音 1〜14 は ±0.6dB で合う）。

**2026-09-16 深夜、層の時刻差を実機で測る物差しを作った（唸りの位相）**:
層どうしの立ち上がりの差は、ふつう実機では分けて測れない。ところが
**同じ波形の 2 声がわずかに音程違いで重なる音色**なら、唸り（うなり）の位相が
その時刻差をそのまま映す。しかも搬送波と唸りの比（051 では 186 倍）に拡大されるので、
**1 標本より細かく**読める。

キーオンのログから 2 声の音色を洗って、次の 3 つを選んだ（scratchpad の twovoice.py）。

| 番号 | 名前 | 2 声の音程差 | エミュの間隔 |
|---|---|---|---|
| 051 | Deep Choir | 9 セント | 41 標本 |
| 043 | Digi Bizzar | 11 セント | 42 標本 |
| 035 | Phase Strings | 16 セント | 9 標本 |

**051 が物差しになる**。同じ音を 6 回鳴らして実機で録ると、唸り 5.587〜5.593Hz・
位相 −23.5〜−28.4 度（5 回中 4 回）で揃う。**位相 ±2 度 = 層の時刻差 ±0.25 標本**。
043 は逆に実機側が 115 度（±27 標本）もばらつき、こちらは固定なので、
**実機では層の時刻差そのものが音によって変わる**ことも分かった。

**測った結果: こちらの層の間隔は 051 で 13〜17 標本長い**（実機 28〜29 標本に対して 41）。
キーオンを人工的にずらす仕掛けで確かめると、**−12 標本で位相が ±10 度（±1.2 標本）まで
合う**。

間隔 41 標本の出どころは書き込みの待ち（upstream.md の 36）で、レジスタの書き込みを
**1 サンプルに 1 回**しか受け取らない決まりから出ている。1 声目のキーオンから 2 声目まで、
firmware は 32 本のレジスタを書き、ちょうど 1 標本に 1 本ずつ進む。実機がこれを
19〜20 標本で済ませているなら、待ちの決まりがどこか違う。2 つの形を試した。

| 形 | 051 の間隔 | 100 件の平均 |
|---|---|---|
| いまの形（書くたびに待つ） | 41 | **0.584** |
| 1 サンプルに 1.5 回受け取る | 29 | 0.596 |
| IIR（0x20-0x2b）とミキサ（0x30-0x3f）は待たせない | 28 | 0.599 |

**どちらも 051 の間隔は当たるのに、100 件の平均は良くならない**（1 つ目は 25 件が良くなり
42 件が悪くなる。018 Full Organ は 1.33 → 1.08 と改善する）。つまり
**仕組みは当たっているが、待ちの決まりそのものはまだ当てられていない**。

**2026-09-16 深夜、測り直した（前の「+17 標本」は手順の甘さだった）**: 唸りで測るときは、
**1 音ごとに全チャンネルの音を止めてパフォーマンスを選び直す**必要がある。そうしないと
前の音の状態が残り、実機の唸りの速さ（＝2 声の音程差）まで変わってしまう。直した手順で測ると:

| 番号 | こちらの層の間隔 | こちら − 実機（標本） | ばらつき |
|---|---|---|---|
| 094 Sweepy Plant | 0 | −13.7 | 7.5 |
| 030 Euro Hook | 8 | +6.5 | 1.9 |
| 071 SQ2003 Wheel | 9 | +6.1 | 1.0 |
| 051 Deep Choir | 41 | −3.9 | 9.4 |

つまり**層の時刻差の食い違いは ±6 標本ほど**で、以前思っていたほど大きくない。093 の感度
（±10 標本で +0.2dB）と合わせると、ここが効いているのは 0.1〜0.2dB ぶんになる。

**同じ夜、XG モードでずっと鋭い物差しを作った**（upstream.md の 36 の追記）。パート 1 と 2 を
同じ受信チャンネルにして 1 つのノートオンで鳴らし、左右に振ると、**1 パートぶんの設定時間**が
波形から直に読める。実機 61.4 ± 1.0 サンプルに対してこちらは 79.0 だった。
**書き込み 1 本の待ちを 440 サイクルにして合わせた**（upstream.md の 36。100 パフォーマンスは
0.584 → 0.560）。

**2026-09-17 未明、1 パートの中のエレメント間の時刻差も測った（合っていた）**。
2 エレメントが少しだけ音程違いの XG 音色を探し（xgscan.py で 128 音色のキーオンを走査）、
唸りの位相で比べた。

| 音色 | 唸り | 位相差 | 時刻差 |
|---|---|---|---|
| 088 | 0.909Hz | ±0.5 度 | **±0.2 サンプル** |
| 004 Honky-tonk | 2.295Hz | −3.1 度 | −1.4 サンプル |
| 022 | 7.492Hz | −41.3 度 | −6.5 サンプル |

唸りの速さ（＝2 エレメントの音程差）は 3 桁一致する。**エレメント間は原因ではない**。

**遅れて鳴る層（エレメントが 278・765 サンプル遅れる音色 045・051・052・061）は、
この物差しでは測れない**。実機側が音ごとにばらつくため（firmware が 2.5ms ごとに数を
減らして鍵を押す仕組みそのもののばらつき。051 は 6 回で位相が +11 度から −156 度まで動く）。
**2 エレメントの音程差を 9 音色で洗った（4 件が実機よりわずかに狭い）**。唸りの**速さ**は
位相と違って立ち上がりのばらつきに強いので、こちらで比べた。

| 音色 | 実機の唸り | こちらの唸り | 差 |
|---|---|---|---|
| 004 Honky-tonk | 2.295 | 2.295 | 0.0% |
| 022 | 7.492 | 7.492 | 0.0% |
| 024 | 1.730 | 1.730 | 0.0% |
| 082 | 1.757 | 1.757 | 0.0% |
| 088 | 0.909 | 0.909 | 0.0% |
| 093 | 1.423 | 1.418 | −0.3% |
| 090 | 1.451 | 1.443 | −0.55% |
| 096 | 1.397 | 1.377 | −1.43% |
| 052 | 0.903 | 0.885 | −1.9% |

**外れる 4 件はどれもこちらが狭い**（偏りがある。丸めのばらつきなら両側に出るはず）。
絶対値にすると 0.1 セントほどで音としては効かないが、ピッチの道に系統的な違いがある。

**追って調べたら、ピッチの道に違いは無かった**（唸りの見え方の問題だった）。

まず、音色 088 の 0.909Hz は**2 エレメントの音程差ではなかった**。エミュの中で実際の
歩幅を出すと（`PITCHDBG` で `pitch`・ピッチ EG・LFO・輪の微調整を書き出した）差は
4 単位＝0.709Hz で、0.909Hz のほうは振幅 LFO を拾っていた。音色 024 も同じ（実際の
音程差は 49 単位＝8.8Hz なのに、測っていたのは 0.865Hz の別物）。

残る 090・093・096 は、どれも音程差 8 単位・輪の微調整 0・ピッチ EG も LFO も無しで、
条件が揃っている。2 つの山の周波数を 4M 点の FFT で細かく取ると:

| 音色 | 実機の比 | こちらの比 | 整数表からの予測 |
|---|---|---|---|
| 090 | 1.005568 | 1.005536 | 1.005598 |
| 093 | 1.005446 | 1.005438 | 1.005463 |
| 096 | 1.005358 | 1.005282 | 1.005379 |

**実機は切り捨ての表とぴたり合う**（ずれ 0.002〜0.003%）ので、`pitch_base` の作り方は
正しい。こちらも 0.003〜0.010% で合っている。**唸りで見ると 1.9% の差に見えたのは、
ほとんど等しい 2 つの数の引き算だから**で、もとの速さに直すと 0.01%（0.17 セント）に
満たない。追う価値のある違いではなかった。

唸りで音色を洗うときは、**測っているものが本当に 2 エレメントの音程差かを、
歩幅から確かめてから**にすること。
（scratchpad の xgscan.py・xgbeat.py・beatan2.py、実機の録音は xb1_hw.wav・xb3_hw.wav）
（scratchpad の beatmid.py・beatan2.py・twovoice.py・xglayer.py・tlgap.py・hornenv.py・
xgscan.py・xgbeat.py、実機の録音は beat1_hw.wav・beat3_hw.wav・xl1_hw.wav・horn_hw2.wav・
xb1_hw.wav・xb2_hw.wav）

#### 次に見るところ

* リバーブ RAM の読み書き（`m_ram_read` / `m_ram_write` の詰め方）
* 掛け算と飽和（`mmode` の各枝、`d.clamp`）
* 直流が振幅に比例する＝利得の非対称。**片側だけ 1 LSB 損をする**形を探す
* 包絡線はどの変種でも 0.3% 以内しか動かない（倍音は動かない）ので、
  この辺りを触っても君の「極は 6/256 で正しい」という結論は崩れない

直流は `make test` の指紋（`dc`）に入っているので、触れば差分で見える。

### 4. 不揮発メモリの保存 — 済。起動状態のキャッシュは見送り

**NVRAM を残すようにした**（`src/nvram.h`）。実機の電池で保持されるワーク RAM
（0x400000-0x43ffff、256KB）を、`%LOCALAPPDATA%\S-MU2000\nvram\<プログラム ROM
のハッシュ>.bin` に置く。

先に試して分かったこと:

* **firmware はこの RAM を読んで起動する。** 残した RAM で起動すると、工場出荷の
  初期化を飛ばすぶん起動が 7.9 秒 → 5.4 秒（音の時間）に縮む
* **XG のマスタボリュームまで残る**（絞ってから入れ直すと、ピーク 9779 → 136
  のまま）。実機も同じ firmware なので同じはず
* RAM は起動しただけで 742 バイト変わる

扱い:

| | 読む | 書く |
|---|---|---|
| `gui` / `live` | 起動の前 | 終わるとき（音を止めてから。**起動できていたときだけ**） |
| VST3 | 起動の前 | **書かない**。変えたものは DAW のプロジェクトに残る。何枚挿しても取り合わない |
| `render` / `make test` | 読まない | 書かない。試験は毎回工場出荷状態から |

書くときは一時ファイルに書いてから置き換える。`live` は Ctrl+C でもすぐには
死なず、止めて残してから終わる。工場出荷状態に戻すのは `--factory`（gui と
live）か、gui の右クリックの「工場出荷状態に戻す」。

**起動状態のキャッシュは見送った。** MEG と CPU を速くした結果、起動はもう
壁時計で約 1〜1.5 秒（VST3 で残した設定から 0.78 秒）しかかからない。
キャッシュで縮むのはそのうち 1 秒ほどで、しかも NVRAM を残すと終わるたびに
RAM が変わるので、「この NVRAM から起動した後の状態」はほとんど当たらない。

（**状態の大きさは既に手当てされていた。** `state_pack`（`src/state.h:120`）が
同じバイトの続きを縮めていて、VST3 はその形で保存している。実測で 6.09MB →
472,821 バイト（7.8%）。ここは課題ではない）

### 5. VST3 のイベントバスを 2 本にする — 済

入力バスを `MIDI In A (Part 1-16)` と `MIDI In B (Part 17-32)` の 2 本にした。
CC などのパラメータも口ごとに持つ（B は 8192 番から。A・出力レベル・状態の
番号は保存した曲のために変えていない）。`vst3probe` の `port_b` で、
`render.exe` との包絡線の差 1.0%（全部 A に流すと 24.6%）。

残り: 実際の DAW で 2 本目のバスが選べるか（DAW による）。

### 次: パラメータの層 — 設計した（doc/params.md）

音色・エフェクト・パートの値を名前で読み書きする土台。いまのエディタは
送った値しか知らない。**firmware が XG の問い合わせとダンプ要求に MIDI OUT から
答える**ことを確かめたので、音源の中の今の値を公式の口で読み返す形にする。
作る順と、決めてもらうことは doc/params.md に。

### 6. 速くする — CPU 側（CMT）と MEG は済

**実時間比 58% → 38% まで来た。** 独り占めモードで 128 フレーム（2.9ms）が
超過 0 で回る。

#### 測り方

`build/blocktime.exe <rom> <MIDI> <フレーム数> [秒] [回数]` で 1 ブロックの所要時間の
分布と、CPU・SWP30・その中の MEG の内訳が出る。

**同じ区間を繰り返して中央値を出す。** 起動直後の状態を保存しておき、毎回そこへ
戻して流すので、どの回も同じ仕事になる。**最初の 20 秒は捨てる**。この機械
（Ryzen 7 9700X）では走り始めの十数秒だけ 13-25% 速く出て、その後に落ち着く。
鳴らし続けるときの速さは落ち着いた後のほうなので、そちらを測る。慣らしの後は
回ごとの幅が 1-2% に収まる（1 回きりで測っていた頃は ±10% 揺れていた）。
ビルドごとのコードの並びによる揺れはこれでは消えないので、小さな差は疑うこと。

部分ごとに止めて値段を測った（16 パート同時、256 フレーム）:

| 止めたもの | 基準比 |
|---|---|
| **MEG** | **−39.6%** |
| AWM2 とミキサ | −14.6% |
| スレーブの SWP30（別糸で並走） | −2.2% |

#### 済: CMT が 16 サイクルごとに CPU を止めていた

`doc/design.md` が予告していた**メモリバスの線形探索は犯人ではなかった**
（64KB 単位のページ表にしたら逆に遅くなった。プログラム ROM は最初の領域
なので走査は 1 回目で当たる）。

本当の犯人は CMT。MU2000 の firmware は COR=1・1/8 で走らせていて合わせが
16 サイクルごとに起き、そのたびに CPU を止めて周辺 10 個に予定を聞き直して
いた。「次の予定」の 99.8% がこれ。割り込みは切ってあるので予定に入れる
必要が無く、読まれたときに計算すれば足りる。

**CPU の時間が 46% 減った**（実行ループが 1 サンプルあたり 80.5 周 → 1.5 周）。
命令の切れ目が動くので発音の時刻が最大 1 サンプルずれるが、声の中身
（サンプル番地・ピッチ・形式）は完全に同じ。MAME にも同じ穴があるので
[doc/upstream.md](upstream.md) に控えた。

| ブロック | 前（平均/最悪） | 後（平均/最悪） |
|---|---|---|
| 1323（30ms） | 16.9 / 21.7 | 11.5 / 16.7 |
| 480（10.9ms） | 6.3 / 9.2 | 4.2 / 7.5 |
| 256（5.8ms） | 3.3 / 5.2 | 2.2 / 4.4 |
| 128（2.9ms） | 1.7 / 3.7（2 回超過） | **1.1 / 2.2（超過 0）** |

#### 済: MEG の判定を前倒しした（-42%）

1 サンプルに 384 命令 × 2 チップ。命令ごとの判定のうち、プログラムと番地の
割り当て（`m_map`）だけで決まるものを、プログラムが変わったときに 1 回だけ
解いて命令表にした（`meg_state::build_ops()`）。1 サンプルぶんは
`run_program()` がまとめて回す。番地の解決（`resolve_address()` の 8 段の探索）
も命令表に焼いたので、`map_w` でも解き直させる。命令表は状態の保存に入れない。

音はビット単位で同じ（`make test` 全件一致）。

| 16 パート同時・256 フレーム | 前 | 後 |
|---|---|---|
| MEG（1 サンプル、マスタ） | 4357 ns | **2525 ns** |
| 1 ブロックの平均 | 2.52 ms | 2.03 ms |
| 実時間に対する割合（平均） | 43.4% | **34.9%** |

（この表は慣らしの後の数。前に書いた 38% は慣らし無しで測った値なので、
並べて比べるなら 43.4% のほう）

いまの内訳は **CPU 3.8µs、MEG 2.5µs、AWM2 とミキサ 1.5µs**（1 サンプル）。
次に大きいのは CPU。

**効かなかったもの**（どれも音は同じだった。同じ回り道をしないために残す）:

| 試したこと | MEG |
|---|---|
| いまの形（基準） | 2525 ns |
| 読み先・頭打ち・遅延書き込みを分岐なしにした | 3184 ns（遅い） |
| ALU を種類ごとの関数（640 通り）にして命令表から呼んだ | 2802 ns |
| 遅延の輪を局所変数の送りにした | 3552 ns（遅い） |

この機械（Zen 5）では分岐予測が 384 命令の周期をかなり当てていて、分岐を
消しても得にならない。部分ごとに止めて測ると、ALU が 36%、遅延書き込みの
反映が 15%、メモリ操作が 6%、dm/dr の書き先が 5%。重いのは分岐ではなく
「p が命令から命令へ直列に流れる」ことそのものと見ている。

プログラムの中身の数え（dense で書き出したもの）: 384 命令のほぼ全部が
掛け算（mmode 2）、ALU の組み合わせは 1 チップで 33〜35 種類、
メモリ操作はマスタ 68 命令・スレーブ 0。

#### 済: MEG の JIT（2026-09-14）

`src/mame/sound/swp30_jit.cpp`。firmware が書いた MEG のプログラムを、プログラムか番地の
割り当てが変わるたびに x86-64 の機械語に訳す（Windows x64 のみ）。訳した物はどこにも
残さない（firmware 由来のものを配らないため、実行時に作って捨てる）。

* 命令ごとの判定は訳すときに決める。3 命令遅れの書き込みと 2 命令遅れのメモリポート・t の値は、
  輪のどの枠かが訳すときに決まるので反映する位置に直に置く。サンプルを跨ぐ頭と終わりの
  数命令だけ輪そのものを使う
* 定数・番地表・LFO は実行時に読む。乱数は同じ順に同じ回数引く
* 分岐（bit 0x3f）を含むプログラム（LO-FI・DYNA 系）は、輪を毎命令で読み書きする形で訳す（2026-09-15 から。前は解釈実行に任せていた）
* `SMU2000_MEG_JIT=0` で切れる

**音はビット単位で同じ**: `make test` 全件、PFM の 100 パフォーマンス（C4）、XG でバリエーション
116 種類をインサーションで・リバーブとコーラスの全種類を、JIT 有りと無しで描いて WAV が
1 バイトも違わない。

| dense・512 フレーム | 解釈実行 | JIT |
|---|---|---|
| MEG（マスタ、1 サンプル） | 1893 ns | **731 ns** |
| MEG（スレーブ、別糸） | 1709 ns | **480 ns** |
| 1 ブロックの平均（実時間比） | 29.7% | **24.3%** |

前に試して遅くなった「ALU を種類ごとの関数にして命令表から呼ぶ」（上の表）と違い、
p が命令から命令へ流れる所を呼び出し無しの一続きの機械語にしたのが効いた。
残りで大きいのは CPU（3.1µs）。Mac（arm64）版の JIT はまだ無い。

#### 済: MEG の JIT の定数の焼き込みと、書き込みの前倒し（2026-09-15）

測ると（自作のサンプリングで、dense、1 本の糸）時間の 41% が MEG の JIT、25% が SH2 の JIT、33% が C++ だった。
MEG の中では、ディザ付きでレジスタへ書く命令が 35%。ビット単位で同じまま、次をやった:

* **定数を焼き込んだ版を別に作る**。firmware は MEG の定数をエフェクトを組むときにまとめて書き、そのあとはほとんど変えない
  （dense で 30 秒に 3000 回、PFM でパフォーマンスを替えるたびに約 1200 回）。定数が 8192 サンプル変わらなければ、
  今の定数を即値にした版を作って使い、1 つでも変わったらすぐ定数を読む版に戻す。係数 0 の掛け算は省き、
  p が変わらない命令（`p = 0 * x + p`）は丸ごと省く。変わった数は swp30_device が持つ（状態の保存の並びは変えない）
* **3 命令遅れの書き込みを、読まれない所では書いた場所で直に入れる**。読む命令の 1 つ前か 2 つ前に同じレジスタへの
  書き込みが無いレジスタだけ（サンプルを跨ぐ分と、終わりの 3 命令が書くレジスタは除く）
* 乱数の種を回しているあいだレジスタに置く。pack24 の限界を即値でなくレジスタから使う

dense で MEG がマスタ 約 773 → 約 650ns、スレーブ 627 → 約 500ns（揺れ 2〜4%）。`SMU2000_MEG_BAKE=0` / `SMU2000_MEG_EARLY=0` で切れる。

同じ夜に続けてやったこと（どれもビット単位で同じ。make test、PFM 100 個、XG の効果全種類 470 秒、1 サンプルごとの
CPU レジスタの畳み値、保存した状態のバイト列で確かめた）:

* MEG: 飽和の限界もレジスタに置いた。LFO の値（`get_lfo`）を関数を呼ばずに機械語で求める
* SH2: レジスタだけの命令が続くあいだは pc を書かない（`SMU2000_SH2_LAZYPC=0` で切れる）。
  遅延スロットの命令も、pc を使わない普通の命令なら機械語で書く（`SMU2000_SH2_SLOTNATIVE=0`）。
  解釈実行に落ちていた命令の大半が、rts や bsr のあとの遅延スロットの NOP・`mov.l @r15+` だった
* SWP30: ミキサは振り分け先のある入力だけ回す。着いているピッチ EG は関数を呼ばない

| | 前（2026-09-15 の夜の頭） | 後 |
|---|---|---|
| PFM を 1 本の糸で 76 秒ぶん描く（m1.mid） | 約 9.4 秒 | 約 7.3 秒 |
| dense を 16 台同時に 20 秒ぶん | 5.38 秒 | 4.91 秒 |
| dense 1 台（別スレッドあり）20 秒ぶん | 2.42 秒 | 2.30 秒 |

続けて（どれもビット単位で同じ）:

* MEG の JIT: リバーブ RAM の詰め方と戻し方を分岐なしにした（符号は音しだいで予測が外れる）。PFM 76 秒で約 2%
* MEG の JIT: 分岐を含むプログラム（LO-FI・DYNA 系）も訳す。飛ばす位置は swp30_device に持つ（保存しない）。
  飛ばされた命令は輪に入れる書き込みを消して t の値だけ入れる。エフェクト 116 種類の 480 秒で解釈実行と WAV が同じ、34.9 → 33.7 秒
* ミキサの足し込みの分岐をなくした（減衰なし = 減衰 0、0xff 以上は並びに入れない）
* MEG の LFO を書いたときに命令表と JIT を作り直さない（LFO の値は実行時に読む）。PFM の描画では差が見えなかった

見送ったもの:

* 64 声の輪の頭（鳴っているか・ピッチ EG）を印のビットで飛ばす。envelope の状態を変える所が多く、印の付け忘れで
  ずれる危険に対して 3% ほど
* firmware の待ちのループ（0x115e0e、CMT のコンペアマッチの印を読んで回る）の早送り。PFM の演奏中の CPU は
  主に 0x127746 からの 64 要素を回す本物の仕事で、待ちは数 %
* MEG の JIT は 1 命令あたり約 7 クロックまで来ている。掛け算・ディザ・24bit への詰めがあるので、ビット単位で
  同じまま大きく縮める余地は少ない

#### 済: ミキサの振り分けを前もって解く（2026-09-14）

1 本の糸で回して 1ms ごとに命令の位置を取ったら、ミキサ（`mixer_step`）が 12% あった。
毎サンプル 96 入力 × 16 出力の route を解いていたのを、route / vol が書かれたときだけ
入力ごとの「足し先と減衰」の並びにするようにした。SWP30 マスタが 1979 → 1750 ns。

#### 済: SH2 の JIT（2026-09-14）

同じ取り方で CPU が全体の約半分。firmware の熱い所は 0.2ms ごとに 64 声・16 パートの作業域を
回す本物の仕事で、暇な待ちループではなかった（飛ばせない）。そこで `src/mame/cpu/sh2_jit.cpp`
でプログラム ROM の上の命令を分岐までのひと続きごとに x86-64 に訳す（Windows x64 のみ、実行時に作って捨てる）。

* 1 命令ごとに `execute_run()` の 1 周と同じ順で「pc を進める → 実行 → 割り込みの印を見る → icount を減らす」をする
* よく使う命令（実行回数の約 97%）は機械語で直に書く。メモリはプログラム ROM とワーク RAM だけを直に触り、
  ほかは解釈実行と同じ関数を通す。周辺に触った命令のあとで印や pc の変化があればブロックを抜ける
* 遅延スロットの命令・珍しい命令は `execute_one` を呼ぶ。m_delay か割り込みの印が立っているときは入らない
  （初めは印を見ずに入って、起動中の割り込みが 1 命令遅れた。chord と dense の指紋で見つかった）
* `SMU2000_SH2_JIT=0` で切れる。`=1` では全部 `execute_one` を呼ぶ。`SH2_JIT_TRACE=サイクル,命令数,ファイル` で
  JIT と解釈実行を同じ形で 1 命令ずつ書き出せる（食い違いを探す用）

**結果は同じ**: `make test` 全件、chord と dense で 1 サンプルごとの CPU のレジスタ、PFM の 100 パフォーマンス（C4）の
WAV が、JIT 有りと無しで 1 バイトも違わない。

| dense・256 フレーム | 前 | 訳しただけ | ブロックを直に繋ぐ |
|---|---|---|---|
| CPU（1 サンプル） | 3070 ns | 1060 ns | **約 870 ns** |
| 1 ブロックの平均（実時間比） | 21.5% | 12.8% | **約 11.7%** |

ブロックを直に繋ぐ: ブロックの終わりで、jit_run が見るのと同じこと（icount が残る・m_delay と印が 0・
次の番地が訳してある）を機械語で確かめて、次のブロックへ飛ぶ。C++ に戻って表を引き直す手間が消える。
ミキサの作り直しも、書かれた入力の分だけにした（firmware が音量とパンをよく書くので、全部だと 4% あった）。

その後、MEG の JIT でリバーブ RAM の詰め方・戻し方と係数の広げ方（m1_expand）を関数を呼ばずに機械語で書いた
（詰め方のループはビット検索に）。verify で C++ の関数と全部の入力を突き合わせる。MEG マスタ 710 → 632 ns。
ここまでで dense・256 フレームの実時間比は平均 **11.1%**。

まだ速くできる所: レジスタを x86 のレジスタに置く、icount をまとめて引く、声の生成（awm2_step、14%）、ミキサ（9%）、
MEG の LFO（呼び出し、2%）。
PGO（`-fprofile-use`）は SWP30 に 12% 効いたが、ビルドの手順が重くなるので入れていない。

#### 外からの指摘（2026-09-13、Mac で動かした人）

Nuked-SC55 を大幅に軽くした人が、このリポジトリを Mac（Standalone / AU / VST3）で
動かして、X で次のことを書いていた。

* オーディオ処理を別スレッドに逃がしていて、UI とオーディオ・MIDI が分離していない。
  分離したら CPU メーターが振り切れた
* 重さの一因は SWP30 のエフェクト処理。DSP をそのまま再現していて、1 サンプルに
  384 命令 × 2 チップ
* Hall Reverb を解析させたらふつうの APF + Comb で、C++ で書けば 1% もいかない。
  ただしエフェクトは 100 種類以上ある
* SH2 のほうも重そう

こちらの見立て:

* VST3 は `process()` の中で直接作っている。別スレッドは読み込み直後の起動だけ。
  gui.exe は WASAPI のイベント駆動の糸で作っている。見たのが古い版か、どこを指しているかは未確認
* **UI の分離が甘いのは本当。** LCD の点の絵（`driver::publish_now` の
  `lcd.render()`）を 25ms ごとに音声スレッドで作っている。firmware の状態を
  写すだけにして、絵にするのは画面側へ移せる（重さへの効き目は小さい）
* 数字は当たっている。内訳は上のとおり CPU 3.8µs・MEG 2.5µs・AWM2 とミキサ 1.5µs。
  Apple Silicon で 1 コアに収まっていないなら、音声スレッドで締め切りを落として
  振り切れるのは自然
* **エフェクトの手書き（HLE）はしない方針。** ビット単位で実機と一致しなくなり、
  100 種類以上を 1 つずつ実機と突き合わせることになる。issue #1 の 3 件
  （LFO・パン・フィルタ）は、どれも「実装を実機と測って合わせる」ことで見つかった。
  この物差しが使えなくなる。音の再現を優先する（持ち主の方針）
* 正確さを保って速くする道:
  * **MEG の JIT**: firmware が書いたプログラムを、その場で x86-64 / arm64 の機械語にする。
    全エフェクトに一度で効き、結果は今と同じ。プログラムが変わるのは種類を変えたときだけ
  * **SH2**: firmware の暇な待ちループを見つけて飛ばす、ブロック単位でまとめて実行する
* Mac 対応の変更を共有してもらえるなら、取り込みを相談する余地がある（持ち主の判断）

---

## 残りの課題

### 音の正しさ

| | |
|---|---|
| ピッチ EG の細部が未確認 | 下の「ピッチ EG の残り」 |
| **XG の変化音色 1225 種を実機と比べた（2026-09-17）** | XG モードで選べる音色の記録を、firmware と同じ表（src/xg/voices.h の lookup）でバンク × プログラムから引いて重複を除くと 1353 種（MSB 0 が 1193、MSB 48 が 101、MSB 64 の SFX が 58、MSB 1 が 1）。比べてあった GM の 128 を除く 1225 種を、送り 0・C4・ベロシティ 100 で 1 秒ずつ実機とエミュで鳴らし（scratchpad の voicelist.py・voicesweep.py・voan.py）、頭 0.1 秒・0.1-0.5・0.5-1.0・離した後 0.25 秒の 4 窓を 1/3 オクターブで比べた。エミュは実機の録る経路（12.04dB 下げて 16bit の 0 側へ切り捨て）をまね、実効値 32 LSB 未満の窓は最下位ビットの丸め方で決まるので比べない。**1109 種が差 1.5dB 未満**。残る 116 種を、実機で 2 回ずつ録り直したもの（vorep.py）と、エミュの鳴らし始めを 0 / 0.625 / 1.25 / 1.875ms ずらした 4 本（firmware の 2.5ms の刻みとの位相）と突き合わせると（judge.py）、60 種は実機同士でも同じくらい揺れ、エミュのずらしだけで揺れるものも多い（重ねた要素のうなりの位相で決まる音色。PFM のうなりと同じ性質で、1 回の比べでは決められない）。**実機同士でもずらしでも揺れないのに実機と違うのは 26 種**。**→ 同じ晩に大半を直した**: 差の出た音色はどれも要素自身が音量の LFO を持ち、三角波の音量側を中央から始めていたのが原因（upstream.md の 38、d6de416）。0/21/38・0/64/44・0/65/44・0/127/44・0/69/90・0/70/7 が 0.0〜0.1dB になり、1225 種で差 1.5dB 未満が 1109 → 1122。直した後も「実機同士でもずらしでも揺れないのに実機と違う」のは 14 種: 48/48/2（13.1、実機同士 6.1）・48/88/7（11.3）・**0/77/101（8.6。直す前は 3.0）**・64/0/37（7.9、実機同士 4.1）・0/68/90（5.5）・48/88/6（5.0）・0/66/53（3.3、離した後の帯の形）・0/66/97（2.4）・0/40/50（2.3）・64/0/56（2.1）・0/75/55（1.9）・0/40/81（1.8）・0/97/15（1.7）・0/66/18（1.6）。0/77/101 は 4 要素で、すぐ立ち上がる 2 つ（アタック 0x77）と、ゆっくり立ち上がり速い三角波の音量 LFO（深さ 0x18、速さ 0x2F）を持つ 2 つ（フィルタ B が 0xF800）。差は頭 0.1 秒の 1.2〜7kHz の帯だけで、エミュが直す前 -4〜-8dB、直した後 -8〜-11dB。0.1 秒より後は直す前も後も ±0dB。直す前から合っていない高域の立ち上がりが、LFO の位相で見え方を変えたもの（scratchpad の bandwin.py）。GM の 0/0/37（Slap Bass 2）は直す前も後も頭から +5〜+11dB 大きかったが、**要素の鳴り始めの間隔のばらつき**だった: この音色は 2 要素（先に鳴る要素と、7〜8ms 遅れて鳴るアドレス 0 の波形）で、2 つが干渉する。エミュは firmware の 2.5ms の刻みとの位相で間隔が 312〜376 サンプルに散り、330 なら 50ms ごとに 66 63 58 54 51 と静かに速く減り、376 なら 69 68 65 62 60 と大きくゆっくり減る。実機は GM 128 の流れの中でも単独でも 330 の形と同じ（録る経路の 12dB を足して 66 63 58 53 51）。GM 128 を順に鳴らすと 37 番目はスレーブの ch23/24 に割り当てられ、間隔 358 になって大きく出ていた（スレーブかどうかは関係ない。--single でも同じ）。PFM のうなりと同じ「遅れて鳴る層の時刻」の問題（scratchpad の pfmdbg.cpp、envone.py、keyctx.py）。**続き（同じ朝）**: 鳴らし始めを 1 回ごとに 0.104ms ずつずらして 24 回（2.5ms の刻みを 1 周）実機とエミュで鳴らし、エミュの回ごとの間隔（キーオンの記録）と包絡の対応から、実機の各回の間隔を一番近い包絡で見積もった（scratchpad の gap37.py）。エミュは 275〜379 サンプル（幅 104、平均 333）で回ごとに -4.5 ずつ進んで折り返す。実機も散っていて、見積もりは 275〜362（平均 314。275 に当たった 4 回はそれより短いかもしれない）。**幅は同じで、実機のほうが約 20 サンプル（0.45ms）早く 2 つ目の要素を鳴らしている**。エミュで一番大きく鳴る 366〜379 は実機に出ない。実機で 4 回とも 330 の形に見えたのは、散り方の近い位置を引いただけ。440 サイクルの待ちを入れて平均が合ったフレンチホルン（upstream.md の 36）とは食い違うので、待ちの長さか、遅れて鳴らす要素の書き込みの本数の数え方にまだ違いがある。ただし注意が 2 つ: (1) 包絡は 2 要素の相対位相で決まるので、C4 の 1 周期（約 169 サンプル）違う間隔は同じ包絡に見え、「20 サンプル早い」と「約 150 サンプル遅い」を区別できない（フレンチホルンは要素の波形を録音に当てはめる delfit.py で測ったのでこの曖昧さが無い）。(2) 2 つのキーオンの間の待つ書き込みは、フレンチホルンも Slap Bass 2 も同じ 7 本・同じレジスタ（scratchpad の between.py）で、本数の違いでは説明できない。確かめるには Slap Bass 2 でも delfit と同じ当てはめで実機の間隔を直に測る。**→ 試したが使えなかった**: エミュの要素ごとの出力（--dump-dac）を「A + d サンプル遅らせた B」で当てはめる delfit37.py は、正解の分かっているエミュの録音でも正解から 130〜370 サンプル外れ、残差も 4〜12% 残る。この音色は firmware が 10ms ごとに両要素のフィルタ（スロット 0x00・0x01）を書き換えるので、要素の波形そのものがキーオンと 10ms の刻みの位相で変わり、別の回で取った波形を当てはめられない（フレンチホルンはこの書き換えが無い）。以下は直す前の記録。はっきりしているのは 0/68/90（頭 0.1 秒が -7.1dB）・0/69/90（-6.1dB）・0/21/38（-3.8dB）・0/70/7（-2.4dB）・64/0/34（0.1-1.0 秒が -3dB）と、プログラム 44 の LSB 64/65/127 が揃って頭 +1.5〜2.0dB。0/69/90 は実機もエミュも約 290ms 周期で音量が上下し（2 つの要素のうなり）、周期は同じで位相だけエミュが約 70ms 早い。エミュでは 2 つの要素は同じキーオンの書き込み（マスク 0x0006）で同時に鳴り始め、ピッチ EG（-7 と +11 から速さ 8 で 0 へ）も入っている。ピッチ EG のずらし方を変えてもこの 3 種は 0.1dB も動かないので、原因は別（要素ごとの波形の読み始めの位置か、firmware が 10ms ごとに書き換える音程の届き方か）。一覧は scratchpad の real.txt |
| **バリエーションを送り（SYSTEM）で使う形を実機と比べた（2026-09-17、合った）** | 接続（02 01 5A）を SYSTEM にし、ch1 の Square Lead の A3 を送り（CC94）127 で 2 秒鳴らして、DELAY LCR・DELAY L,R・ECHO・CROSS DELAY・ER 1・GATE REV・KARAOKE 1・ROTARY SP・TREMOLO・AUTO PAN・PHASER 1・DISTORTION・3-BAND EQ・AUTO WAH・CELESTE 1・HALL 1 の 16 種を比べた（revsweep.py の variation、revan.py）。鳴っている間は量 ±0.1dB・形 0.05-0.5dB、左右の相関は全種類で小数 2 桁まで一致。離す瞬間の窓が ±1dB、ECHO の 16bit の床近くの尾だけ -2.9dB。インサーションとしての 116 種は 2026-09-15 に比べてあるので、送り・戻り・パンの道も合っている。後始末は種類 DELAY LCR、接続は資料の既定の INSERTION（0）へ戻した（実測の既定ではない） |
| **128 声を超えたときの奪い合いを実機と比べた（2026-09-17、合った）** | ch1・ch2 に Strings（2 要素）を置き、ダンパーを踏んだまま 50ms おきに 90 音（180 声ぶん）を積んで 3 秒伸ばし、離した（scratchpad の poly.py）。0.25 秒ごとの音量は積み上げの 7.5 秒間ずっと実機と +0.1〜+1.1dB（平均 +0.4）、スペクトルの重心は数 % 以内。離した後の減り方も 16bit の床に着くまで同じ |
| **マスターのマルチ EQ を実機と比べた（2026-09-17、合った）** | Saw Lead の C3+C4+C5 を、EQ の種類 FLAT・JAZZ・POPS・ROCK・CLASSIC と、FLAT から低域 +12・高域 -12・中域 +12（Q 120）にした 8 通りで鳴らし、1/3 オクターブ 27 帯で比べた（scratchpad の eqtest.py）。FLAT からの効き方の差は帯の平均 0.11〜0.27dB、最大 0.86dB。後始末は種類 FLAT を選び直す |
| **コントローラの効き方を実機と比べた（2026-09-17、合った）** | 送り 0 で、CC7・CC11・ベロシティ・パン・CC74・CC71・CC74 の掃引・CC73・CC72・CC75・ベンド幅（RPN 0 で 2 / 12 / 24）とベンド・CC1・ビブラートの NRPN（速さ・深さ・遅れ）・ポルタメント（CC5 20 / 80）・ダンパー・モノを 45 区間鳴らし、50ms ごとに音量・左右・スペクトルの重心・音程を比べた（ctlsweep.py・ctlan.py）。音量とパンは鳴っている間どの区間も 0.1〜0.5dB、音程はポルタメントの軌跡もベンド幅 24 の 219→876Hz も 3 セント以内。暗く小さい音ほどエミュの重心が高く出る（CC74=0 で 33%、ベロシティ 50 で 25%、127 で 0.9%）のは、エミュを下げてから切り捨てるので量子化の雑音が 2 重に乗るためで、音量はうなりの形まで 0.1dB で一致している。CC72=20 の離した瞬間の 14dB は、50ms より短いリリースが 1 枠に入るのでノートオフの送り出しの数 ms で決まる。違うのは、音程を速く動かしたときの 2 要素のうなりだけ（ベンド幅 24 でエミュだけ 3〜6dB 上下する、ポルタメントで上へ滑る間に最大 5dB）。ベンド幅 2 では小さく、動かし方が速いほど大きい |
| **パートの値（SysEx）と CAT・PAT・AC の効き先を実機と比べた（2026-09-17、合った）** | XG モードの実機へ XG System On を入れずに、送り 0 の ch1 で 44 区間を鳴らした（scratchpad の ctlsweep.py の CTL_PARTS、ctlan.py）。ノートシフト（-12 / +7）・デチューン（08 00 09）・スケールチューニング（C と A）・RPN 1 と 2・ベロシティの深さとオフセット・鍵盤とベロシティの下限（鳴らないこと）・キーアサイン SINGLE・MW / PB / CAT / PAT / AC1 / AC2 の効き先（音程 +12、フィルタ、音量、LFO の PM・FM・AM を 1 秒で上げる）・AC1 の CC 番号の付け替え・パートのピッチ EG（08 00 69-6C）。どの区間も後始末で既定に戻し、流す前にエミュで XG の値の塊が元に戻ることを確かめた（ramcheck.cpp）。音量は鳴っている間 0.1〜0.5dB、音程は LFO の PM を最大にした揺れの軌跡まで 1Hz 以内で重なる。目印が付いたのは、立ち上がりが 50ms の枠をまたぐ区間、暗い音の重心、音程の検出のオクターブの取り違えで、枠ごとに見ると合っている。音程を動かす区間（PAT・AC1 の +12）で 6dB 出るのは、コントローラの表に書いた 2 要素のうなりで、実機を 2 回録ると同じ区間が 0.5dB と 6.3dB に分かれる。1 回目の実機の録音だけ、MW で音程とフィルタを動かす 2 区間で音が途中で消えたが、同じ MIDI の録り直しと、その区間の前後を組み替えた MIDI では消えず、再現しなかった |
| **GM 128 音色を鍵盤の端（C1・C8・音 120）で実機と比べた（2026-09-17、合った）** | 変化音色と同じ形（voicesweep.py・voan.py、帯は一番大きい帯から 20dB 以内で採点）で、送り 0・ベロシティ 100 の 1 秒を鳴らした。差 1.5dB 未満は C8（音 108）127・音 120 125・C1（音 24）121。音 120 では音域の外で鳴らない音色が多く、鳴らないことも実機と一致する。C1 を実機で 2 回録って実機どうしで比べると 122 で、Whistle・Ocarina は ±5dB、Tremolo Str・Synth Str 1/2・Kalimba は 1〜3dB 揺れる。実機どうしの揺れより大きく残るのは C1 の Music Box の頭 0.1 秒（形 2.3dB、実機どうし 0.6dB）と French Horn の窓ごとに ±1.2〜1.5dB 入れ替わる差（実機どうし 0.6dB）くらいで、どちらも揺れや層の位相で決まる形。C8 の Shakuhachi の 0.5-1.0 秒の 11.9dB は、実機の録音にだけ全帯域へ 40〜57dB の短い雑音が乗ったもので、倍音の帯は 0.1dB で一致する |
| **フィルタと EG を端まで回した形を 13 音色で実機と比べた（2026-09-17、合った）** | Piano・EP・Organ・Guitar・Bass・Strings・Brass・Sax・Flute・Square・Saw・Pad・FX で、CC74 127 / 0 にレゾナンス 127、CC73 127、CC72 127、CC75 0 と CC73 0 を送り 0 の A3 で鳴らした（ctlsweep.py の CTL_EXT、65 区間）。レゾナンスを上げて開け切る・閉じ切る形も、鳴っている間の音量と音程は合う。CC73 127 は 1.6 秒では 16bit で数 LSB にしかならず比べられないので、CC7 127・ベロシティ 127 で 6 秒伸ばす形（CTL_ATK、6 音色 × アタック 96 / 112 / 127 とリリース 100 / 127）で録り直すと、アタックの上がり方もリリースの減り方も、実機で 5 LSB を超える所から先は 0.1〜0.5dB で重なる。Brass（61）だけ、ゆっくりした揺れの周期は揃ったままエミュが 0.5〜1.2dB 小さく出るが、同じ区間を 2 回鳴らすと 2.4dB と 0.9dB に分かれるので、揺れの位相で決まる差。明るさ（重心）がエミュで 13〜20% 高いのは、コントローラの表に書いた、エミュを下げてから切り捨てる量子化の雑音 |
| **12 パートの曲の形で実機と比べた（2026-09-17、合った）** | 実際の曲に近い 32 秒の試し曲（scratchpad の songgen.py。ドラム・ベース・パッド・EP の刻みと途中の音色替え・CC11 で膨らませるストリングス・ギター・ブラス・ペダルを踏むピアノ・CC1 を掛けるオルガン・ベンドとモジュレーションのリード・サックスとフルート、送りあり）を、XG System On を入れずに鳴らし、250ms の枠を 1/3 オクターブで比べた（songan.py）。音量の差は中央 -0.03dB・枠の |差| の平均 0.24dB、実機どうし（2 回録り）は 0.14dB。帯ごとの平均は、エミュだけ 100Hz で +0.6〜0.7dB・252Hz で +0.5dB・400Hz で -0.4〜-0.6dB ずれ、実機どうしは全帯 ±0.2dB。送りを全部 0 にしても、曲全体を 0.625ms 刻みでずらしても変わらない。パートを 1 つずつ鳴らすと（ベース・ドラム・パッド・EP・ストリングス・ギター・ピアノ）どれも 100Hz の差は 0.0〜0.3dB に収まる。1 つずつ鳴らした形はマスタの SWP30 しか使わず、全部合わせるとスレーブも 64 声使うので、ch14 に音量 0 の音を 60 個押さえてベースをスレーブへ回した（エミュの記録でスロットがマスタの 13-28 からスレーブの 57-63 へ移るのを確かめた）が、実機は押さえない形と相関 0.999・帯 0.0dB で同じ音、エミュとも帯 0.2dB 以内で合い、スレーブの経路は原因ではなかった。ベースとドラムだけでは 100Hz が +0.3dB で、エミュでベースだけを -1 / +1 / +2.5ms ずらすと +0.6 / +0.1 / -0.4dB と動く。同じ時刻に鳴るパートどうしの 1ms 前後の発音の時刻の違いで、低い帯の打ち消し合いが変わる大きさで、音源の違いではない（相互相関で見ると、ノートごとの実機とのずれはベースで 0.5ms、ドラムで 1.5ms ほどの幅に散る） |
| **ドラムの楽器ごとの NRPN を実機と比べた（2026-09-17、合った）** | ch10 のスネア・キック・クローズハイハットで、音程（40 / 90）・音量（20 / 127）・パン（0 / 127）・カットオフ（20 / 110）・レゾナンス 120・アタック 120・ディケイ（20 / 110）を 1 つずつ変え、後始末はドラムセットアップのリセット（00 00 7D 00）。大きい枠はどれも 0.2〜1dB。パン 0 で左右が ±35dB 違うのは XG のドラムのパン 0 がランダムのため。ディケイ 110 のキックは 5ms 未満で消えるので、枠の区切りで量が大きく変わる（スネアでは符号が逆に出る） |
| **XG の変化音色 1225 種を C6・C2 でも実機と比べた（2026-09-17）** | C4 と同じ形で C6（音 84）と C2（音 36）も録った（scratchpad の vskeys.ps1）。純音に近い音色では、倍音の間の帯で実機に音より 30〜50dB 下の広い雑音の床があり（エミュはもっと静か）、帯の形の物差しがそれに引っ張られていた（0/126/71 の C6 は倍音の帯が全部 0.4dB 以内で一致）。一番大きい帯から 20dB 以内の帯だけで採点し直すと、差 1.5dB 未満は C4 1124・C6 1137・C2 1110。3 つの高さ全部で差が出るのは 37 種で、刻みとの位相で揺れるものを除くと **MSB 48 の音色**が目立つ（48/48/3・48/48/4・48/88/4〜8・48/80/0。一覧は scratchpad の allpitch.txt）。48/48/3 は 1 要素で、27〜168 サンプルの短いループ（C6 では 8bit の矩形波がちょうど 1 周期）を鳴らすが、**実機だけ 1 オクターブ下の成分がある**（C4 で 126Hz 帯が実機 70.7dB・エミュ 33.7dB、本来の 252Hz 帯は両方 76dB。C6 では 504Hz 帯が実機 70・エミュ 29）。エミュの音程の値の読み方（14bit 符号付き、1 オクターブ 1024）は正しく、エミュは本来の高さで鳴る。48/48/4（1 要素、12bit・168 サンプル、音程 +0x3FC）は C4 で山が実機 317Hz 帯・エミュ 400Hz 帯と長 3 度ほど違う。48/48/2 は合う。どちらも波形のアドレスは 0x3F6xxx・0x3FDxxx。48/48/3 の C6 は頭の長さとループの長さがどちらも 27 で、エミュが頭として読む 27 サンプルは斜めに立ち上がる形、ループは平らな矩形波。「頭とループを続けた 54 サンプルを繰り返す」を一時的に入れると C6 の差は 40.7 → 8.45、C4 は 17.5 → 11.4 に近づいたが、**同じ波形（C4 で 168 サンプル・圧縮、C6 で 27 サンプル・8bit、同じ番地）を同じ音程の値で使う 0/82/38・0/88/38・0/66/118 などは、エミュのままで実機と差 0.0〜0.1** なので、読み方の違いではない（3 つの高さで頭とループの長さが等しい要素を持つ音色は 9〜14 種、そのうち差が出るのは 48/48/3 だけ。scratchpad の eqcross.py）。0/82/38 と違うのはフィルタ（0x00 が 163D/14F2、0x04 が 2000/D000）、アタック（0x06 が 3A7E/7700）、ディケイ・リリース、LFO の速さ（0x0A が 7A00/6000。深さはどちらも 0）で、どれも 1 オクターブ下の成分を作るものではない。0x04（レゾナンス）が 0x2000（ダンピング p2 = 8 の鋭い共振）で、フィルタの内部の値が最大 259 万と 22bit の範囲を少し越えるので、実機の語長での頭打ちが周期を倍にする可能性も試したが、h・b・l を 22bit / 23bit で飽和させても48/48/3 は 17.5 / 41.4 から動かず、22bit では同じ回のほかの音色が悪くなった（183 → 181、181 → 178）。原因は分からない（bandone2.py、elemreg.py、pfmdbg.exe の PFM_DUMP） |
| **システムのリバーブ・コーラスの種類を実機と比べた（2026-09-17、合った）** | XG モードの実機（XG INIT のあと）へ、XG System On を入れない MIDI で、リバーブ 18 種（HALL 1〜BASEMENT）は ch10 のスネアを送り 127、コーラス側 18 種（CHORUS 1-4、GM CHORUS 1-4、FB CHORUS、CELESTE 1-4、FLANGER 1-3、GM FLANGER、SYMPHONIC）は ch1 の Square Lead の A3 を送り 127 で 2 秒通し、種類ごとに 1/3 オクターブで比べた（scratchpad の revsweep.py・revan.py）。エミュは実機の録る経路（12.0dB 下げて 16bit の 0 側へ切り捨て）をまねてから比べる。リバーブは音の頭から 0.8 秒までが量 ±0.1dB・形 0.01-0.04dB、0.8-1.8 秒が ±1.2dB、左右の相関は全種類で小数 2 桁まで一致。1.8 秒より後（-60〜-80dB、16bit の床）だけエミュが 2-4dB 静か。コーラス側は鳴っている間が全種類 ±0.3dB。最初に CHORUS 1 を選んだ回だけ -2.2dB・左右相関 0.59/0.25 と外れたが、別の種類の後に選び直すと +0.1dB・0.32/0.30 で合う。実機は既定の CHORUS 1 を何時間も回していて、同じ種類を選び直しても読み込み直さない（LFO の位相が揃わない）ため |
| **エフェクトを選んでから音が戻るまでの時刻（2026-09-17、未解決）** | 音を鳴らしたまま種類を SysEx で選び、12 回を 20ms の刻みの一周に散らして（1 回ごとに 4001.667ms 空ける）、途切れ始め・戻り・刻みの位相を実機とエミュで測った（scratchpad の fxz12.py・slanz.py）。途切れ始めは両方とも SysEx の直後（実機 0.4ms、エミュ -3.6ms）。戻りはどちらも 20ms の刻みに乗る（回ごとに -1.67ms ずつずれて 20ms で折り返す）が、平均で実機が遅い: SLICE は戻り 252.2 / 235.4ms・刻みの位相 +8.9 / -11.2ms（差 17-20ms）、3-BAND EQ は戻り 188.3 / 175.8ms（差 12.6ms）。途切れ始めの 4ms を引くと SLICE 13-16ms、EQ 約 9ms。render --trace-swp に時刻を足して読み込みを並べると、エミュは SysEx の 75-85ms 後に仮のプログラムとミキサ、SLICE だけ 85-163ms にリバーブ RAM へ表を 2052 項目（1 項目は 0x84E を 2 回読んでから番地 2 語・中身 2 語。中身の 2 語それぞれに 440 サイクルの待ち、1 項目 34µs）、その後マスタに何も書かない待ちがあり、最後の書き込みから約 90ms 後の刻みで本物のプログラムを書く（EQ は仮のプログラムの 107ms 後、SLICE は表の終わりの 92ms 後）。「最後の書き込み + 約 90ms の後の刻み」なら、実機の遅れは読み込みがその分長いことを意味し、種類に共通の約 9ms と、表の長さに比例する分（SLICE で +4〜7ms）がありそう。どこが長いのかはまだ見ていない |
| **XG のエフェクトのうち、実機と合わない種類** | **2026-09-15 午後**: **2026-09-15 深夜: 読み込みの時刻を実機で直に測った**（scratchpad の itab/loadtime.py、lt_hw.wav。音を伸ばしたままインサーションの種類を SysEx で切り替え、音が途切れて戻る時刻を 1ms で見る）。途切れ始めは実機 8〜18ms・エミュ 1〜5ms。戻るのは DISTORTION・OVERDRIVE・THRU で実機 175〜195ms・エミュ 165〜184ms（実機が 0ms か約 22ms 長く、切り替えごとにどちらかに分かれる。実機に 20ms 前後の刻みがあるらしい）、SLICE だけ実機 240〜243ms・エミュ 178〜180ms で 62ms 長い。SLICE は読み込み中にリバーブ RAM へ表を 2052 項目（1 項目に番地 2 語・中身 2 語）書き、エミュでは 56〜62ms で終わってから 173ms まで何も触らずに待ち、173ms に本物のプログラムを書いて r26 が動き出す。表の書き込みで CPU を 1 項目 1 回待たせると戻りが +20ms、2 回で +80ms、4 回で +180ms と、実機の +62ms はその間になる。ところが戻りの時刻を合わせる形では刻みの位相が合わず（2 回待たせると戻り +18ms・位相 120ms、実機 9ms）、位相の起点は読み込みの終わりそのものではないらしい。1 つの待たせ方では両方を説明できないので、エミュには入れていない（刻みの位置が数十 ms ずれるだけで、曲のテンポとは同期しない）。 **→ その後に解決**: 位相の起点の測り方（実機の録音の時刻合わせ）を、効果の掛からない音の頭で合わせ直すと、戻りの時刻と位相は 1 つの形で両方合った。MEG のプログラムの中身とリバーブ RAM の中身の書き込みでも CPU を待たせる（upstream.md の 36）。 SLICE の弱い音の +5dB は、刻み（125ms 周期、r26 に定数を足し続ける位相）の形は実機と同じで、位相だけがずれていた。実機で「種類を選ぶ SysEx から弾くまで」を 0〜1 秒ずらして録ると、SysEx から見た刻みの立ち上がりは 125ms の中で 3〜14ms（平均 9ms）、エミュは毎回 34ms。SysEx で DRY/WET を切り替えて測ると効き始めの遅れは実機とエミュで 0.5ms 以内で同じなので、MIDI の経路ではない。エミュでは SysEx の 45ms 後に仮のプログラムと表を書き、52ms から 165ms まで SWP30 に触らずに待ってから本物のプログラムを書く。この待ちが実機より約 25ms 長いらしい（中身は未調査）。読み込みの時点で r26 を 0 にしても音は 1 バイトも変わらない。D.TURNTBL の帯の差は、鍵を離したあとの尾（実機 18dB に対してエミュ 35dB）。2026-09-15 深夜に 20ms 刻みで見た: 実機は鍵を離した瞬間は小さなクリック（-70dB）だけで、120〜180ms 後に 20ms ほどの短い音（-57〜-60dB）が鳴る（離すたびに遅れが違う）。エミュは離した瞬間に -54dB の音がまとめて出て、遅れて鳴る音は無く、その後の床も 3dB 高い。レジスタを追うと、離したあと r34・r36〜r3b が負の大きな値からゆっくり上がる（包絡の追従らしい）。これが閾値を越えたら鳴らす形なら、エミュでは閾値の判定か、上がる速さが違う。材料は scratchpad の fx/dump/0x750x00.*（tt_dump.txt は離す前後 0.18 秒の m/r）。 **→ 同じ夜に解決（違いではなかった）**: D.TURNTBL をかけたまま何も弾かない時間も録ると、実機もエミュも -58〜-63dB の雑音の上に -43〜-49dB の短い「プチッ」が不規則に鳴り続けていて、数も大きさも同じくらい。離した後に鳴って見えたのはこのプチッがたまたま重なっただけで、鳴る時刻は乱数で決まるので実機とエミュで揃わない。残るのは、離した直後の減り方がエミュで 50ms ほど遅い（-52dB が 1 コマ残る）ことだけ。材料は scratchpad の fx/ttsweep.py・ttan.py・tts_hw.wav（頭に全チャンネルの All Sound Off、終わりにインサーションを THRU に戻す）。 **2026-09-16: D.TURNTBL は実機と合った**（scratchpad の tt2.py・tt2an.py・tt2sum.py、tt2_hw.wav / tt2_emu.wav）。XG モードの実機へ、XG System On を入れない MIDI（頭に全チャンネルの All Sound Off、終わりに THRU と受け持ち無しへ戻す）で、長さと強さを変えた 16 音を D.TURNTBL に通して録り、鳴っている間・離した直後・離した後の尾に分けて比べた。時刻合わせは最初の音の頭で取り、録音全体の 2ms のずれを直してから測る（鍵を押した時刻のずれも 16 音中 13 音で -2ms と揃っているので、離し方の違いではなく録音の時刻合わせ）。結果は、鳴っている間の音量 -0.04dB、帯（音量を除く）1.07dB、離した直後 10ms が -0.88dB、離した後 0.02-0.42 秒が 0.90dB。同じ MIDI をインサーション THRU で測ると帯が 0.59dB なので、残る差の半分は音色そのもので、D.TURNTBL の分は 0.5dB ほど。離したあとの波形の形も相関 0.95-0.98 で一致する。以前の「実機 18dB に対してエミュ 35dB」は、その後の 31・34・36 の直しで消えていた。以下はその前の記録。  **2026-09-15 夜の更新**: 下の「表を引く」種類は、表を firmware がリバーブ RAM の 0 番地付近へ直に書いていて、bit 0x23 の読み出しがそこを絶対番地で引くのだった（upstream.md の 24、書く値の目盛りも 8 倍違っていた）。AUTO PAN 2・AMP SIM 2・RING MOD・LOW RESO・D.SCRATCH は実機と合った。残り: MULTI COMP は upstream.md の 29 で ±1dB まで近づいた、SLICE は upstream.md の 30 で合った、D.TURNTBL（帯 7〜12）、DYNA 系（DYNA PHASE・DYNA FLANG の +6dB を含む）は upstream.md の 31 で合った、ロータリーの系統と V-FLANGER は upstream.md の 32 で合った。以下はその前の記録。 2026-09-15、全 116 種類をインサーション 1 にかけて SineLead の A3 を通し、実機（XG モード、2 回録って 2 回とも同じ音）と比べた。ほとんどは音量が共通の差のまま、帯も 1dB 以内で合う。合わないもの: (1) **無音**: AMP SIM 2・MULTI COMP・RING MOD・LOW RESO（RING MOD と SLICE は upstream 24 で直した。内部の表の 0x000-0x0ff を 256 点の正弦とした。0x100 から上の中身は分からない）。どれもメモリの読み出しに bit 0x22-0x23 = 2 と +idx が付いた「表を引く」形で、RAM のその区画は firmware も MEG も書かない（全部 0）。RING MOD の搬送波は、位相 r26 の上位 8bit を idx にして 0x80 を中心とする 256 点で 1 周（実機の搬送波 1001Hz と計算が合う）、AMP SIM 2 は 0x800 を中心とする 4096 点。RAM ではない表（SWP30 内部の ROM？）を読んでいるらしい。2026-09-15 夜に解釈実行で表の添字（番地表の値 + idx）を記録した: AMP SIM 2 は 0x000〜0xfff を全部使い、次の命令で +1 を読む（4096 点を小数部 t で直線補間する波形整形。0x000〜0x0ff の正弦と範囲が重なるのが謎）。MULTI COMP は 0xc00 と 0x1800 に包絡から作る idx を足す（エミュでは入力が無音のため idx 0 のまま）。LOW RESO は 0xef2〜0x110e あたりを動く（係数の表らしい）。表は少なくとも 0x1802 個ある。116 種類のうち表を引くのは 10 種類で、実際の添字（解釈実行で記録）は: RING MOD・DYNA RING 0〜0x100、V-FLANGER 0〜0x1f0、AMP SIM 2 0〜0x1000、AUTO PAN 2 0x200/0x604/0xa08/0xe0c から、SLICE 0x100/0x500/0x900/0xd00 から、D.TURNTBL 0xfef〜0x1011、LOW RESO 0xef2〜0x110e、D.SCRATCH 0xd1e〜0x1301、MULTI COMP 0xc00・0x1800 付近。0x100 から上を引いても実機と合っているのは V-FLANGER と D.TURNTBL（その読み出しの寄与が小さいらしい）。bit 0x22-0x23 は MAME が解いていない。(2) **音量が大きく違う**: SLICE（-33dB。upstream 24 のあと音量は合ったが、実機は 120ms ほどの周期で刻むのにエミュは 0.5 秒の山が 1 つ。刻みの形を内部の表 0x100・0x500・0x900・0xd00 から idx 付きで補間して作っていて、表の中身が無いため）。DUAL ROTR1/2（-17/-25dB）は upstream 26 で直した（idx にも書く命令が mw を取り込んでいた）。(3) **音色が違う**: D.SCRATCH・DYNA RING（どちらも bit 0x22 付きの読み出しあり）、DYNA PHASE・DYNA FLANG（+6dB、分岐と flags を使う。2026-09-15 夜に分岐の解き方を 4 通り試した: 飛ばした命令が輪の枠を消さない・flags を飽和の前で取る → 何も変わらない。n を逆に読む → DYNA PHASE は変わらず LO-FI・V-FLANGER・D.TURNTBL が壊れる。飛ばす先の命令も飛ばす → DYNA PHASE は +8.2 → +3.8dB に近づくが V-FLANGER +19dB・LO-FI -23dB と壊れる。どれも正解ではない。同じ日にさらに 4 通り: p>>15 を足す形を >>16 / >>14 にする、絶対値で止める形を 0 で止める形にする、定数を 8bit 上げる形を 7bit にする → DYNA PHASE はどれも変わらないか遠ざかり、ほかの種類が壊れる）、AUTO PAN 2（+4dB。帯は合う。揺れを内部の表 0x200・0x604・0xa08・0xe0c から idx 付きで補間して作っていて、0x100 から上の表の中身が分からないのが原因らしい）。材料は scratchpad の fx/（fxs.mid、実機 2 回ぶん、types.txt、全種類のダンプ dump/*.m、命令の特徴の数え上げ census.py） |
| **0/51 のモジュレーションの揺れが違う（済み）** | パフォーマンスモードで CC1 を振ると、実機は 1.32Hz で 60 セント前後ゆっくり揺れるのに、エミュは 6.64Hz で 27 セント（CC1 の値に関係なく）。LFO の速さ（スロット 0x0A の step）か、firmware 側のゆっくりした揺れの経路が違う。2026-09-14 の LFO の直し（upstream 15）の前から同じ。 **2026-09-15 夜に見直した: 済み**。9/14 に実機で録った同じ試験（scratchpad の mwtest.py、mw_hw.wav）を今のエミュ（pfmrender）と比べると、0/51 の基音は実機もエミュも 260.6Hz と 261.9Hz の 2 つの層に分かれ、強さの差もどちらも -3dB で同じ（2・3 倍音も同じ並び）。実機の「1.32Hz・60 セント」はビブラートではなく、この 2 層のうなり（1.3Hz）をピッチの追跡が拾ったもので、エミュの「6.64Hz・27 セント」は遅れて鳴る層が消えていた頃（upstream 16 の前）の値だった。今のエミュは CC1 のどの値でも 7.96Hz・25 セントの揺れを見せ、実機の CC1=127 のとき（7.96Hz・23.5 セント）と合う。CC1=0/64 で追跡の結果が実機と違うのは、うなりの位相（遅れて鳴る層の遅れ、上の「GM 128 音色を C2・C6…」の行）の違いによる |
| **8bit 圧縮サンプルの展開器が不正確（済み）** | MAME 自身が「乗算器はバグっていて負に偏るが正確な挙動は不明。スケーリングが 0 か非 0 かで結果が変わり、隠れた状態がある」と TODO に明記。ロングトーンで「ブーン」というノイズが乗る。2026-09-14: 上限で差分を 0 にしていたのをやめた（upstream 17）。XG の Flute は実機の録音と 1dB 以内で合った（D#6 の差はテンポの違いによる測り違いだった）。MUXG2K の hockinsk さんはモードごとの係数の案を持っている（issue #3）。2026-09-14 夜: 差分の減り方を k(差分 - 1)/d（8bit の端数つき）、漏れを 1/128 にした（upstream 19。ROM の 84 本の直流で形を決め、XG モードの実機の録音で漏れを決めた）。残り: モード 0 の OldEP・OldPiano で基音より下（18〜225Hz）に実機に無い成分が +20〜38dB（基音から見ると -50dB 前後。ループ点で積算器と差分を最初に通ったときの値へ戻す案は効かなかった）、AltoSax の音 70 に 112Hz の余計な音。**2026-09-15 午後に録り直した**（送り 0、XG の Old EP・OldPiano・AltoSax・Trumpet・Piano）: 倍音の並びはどれも 0.2dB 以内で合うが、基音より下が Old EP で +14〜20dB、OldPiano で +9dB、Trumpet で +10dB 多い（Piano は一致）。エミュの余計な成分は**ループ 1 周の周波数（Old EP の C4 で 8.4Hz）の倍数に並ぶ線**で、実機には無い。ループのたびに積算器が同じ値に戻っていないため。ROM のサンプルを直に展開して「漏れの無い積分器でループ 1 周の積算器のずれが 0 になる差分の減り方」を探した（k/8 の floor/trunc/四捨五入、-1 などの寄せ、端数の桁 0/4/8、差分の幅の飽和、負の差分の表の片寄り、減らす順番）が、どれも 0 にならない（最良は trunc((d·k-7)/8) で元の 1〜3%）。エミュに入れても基音より下は良くならなかった。漏れを 1/64・1/32 に強めると下は減るが倍音がずれる。展開の形そのものが違うらしい。道具は scratchpad の dp/（xgdry.py、dpsum.py、dpdec.py）。**2026-09-16 未明: 素のピッチ（音程レジスタ 0、補間なし）で実機を録り、ROM から直に展開した列と 1 サンプル単位で比べた**（dp/natfit.bin、nlan.py、hphase2.py、phase48.py、floorcmp2.py、octbands.py）。わかったこと: (1) **実機の積算器に漏れは無い**。倍音の位相に 1/f の成分が無く、エミュとの差は等価な高域通過で -55〜-60Hz（エミュの 1/128 の漏れ 55Hz の分）。Old EP n48（基音 151Hz）では基音が実機の方が 0.5dB 大きく、これも漏れの分。(2) それでも実機はドリフトせず直流も出ない。ループ 1 周の符号の和は k/d から n48 +24、n60 +13.75、OldPiano n60 -54.9 とずれていて、固定の偏りでは釣り合わない。(3) 余計な成分はループ前にもあり、n60 では符号の時間軸で約 300Hz より下だけ（700Hz より上の倍音の間は実機と一致）。1 オクターブ上・下で録ると境目が 2 倍・半分に動くので、出力側ではなく展開の中の違い。(4) 効かなかったもの: 差分の丸め（floor/trunc/四捨五入、寄せ、端数 0〜12bit、係数 m/256、シフトの和、δ-(δ>>3)、切り捨てた端数の持ち越し、32bit の語の境目でだけ丸める、スケール後の単位で持つ、2^m の粗い刻み）、表（符号ごとに自由な値にしても低い成分はほぼ減らない）、差分の飽和、正負で違う減り方、符号で変わる偏り、積算器の非線形な漏れ、上限の扱い、ループ点での状態の戻し、ROM の中の別の符号化（無かった）。**浮動小数の線形の展開でも同じ量の低い成分が出る**ので丸めの違いでは説明がつかず、線形の違い（高域通過など）は倍音の位相で否定される。仕組みはまだ不明。**同じ日にほかのモードも素のピッチで録った**（dp/mbands.py、m1/m2/m3_hw.wav。ベンド幅 12 でピッチレジスタを 0 に）: モード 1（XG 0/65/1、da28aca0、スケール 3）は 10〜1000Hz で +8〜31dB、モード 2（0/66/61、dc624a74、スケール 3）は 10〜290Hz で +9〜33dB 多く、どちらも Old EP と同じ傾向。**モード 3（0/66/49、de2f7e40）は全帯で 0.1dB 以内、倍音の位相も一致**。表・スケール・積算器の側は正しく、違いは差分を減らす側（モード 0〜2 だけにある経路）にある。**続き（同日）**: (5) エミュと実機の倍音の間の差を展開の伝達 |G|² で割って符号の単位に直すと、4 本とも周波数によらずほぼ平ら（白色）で、丸め程度（σ 0.2〜0.45 符号）の毎サンプルの食い違いが積分されて低い帯で目立っている形（dp/epsd.py）。(6) n48 のループを 127 周平均して実機の実効の符号を推すと、150Hz より下は「ROM の符号の低い成分を抜いた列」（相関 -0.99）。ループの中の ROM の符号の低い成分は 0.024〜0.039 で、白色の丸めの残りの大きさとそろう（符号化器が自分の展開の丸めを打ち消すように符号を選んだ見方と合う）。(7) その前提で ROM だけを使う物差し（候補の丸めの残りの低い帯 + 符号の低い帯 → 0）を作り（dp/rsearch4〜7.c）、ビーム探索で符号化した合成データで正解が相関 -0.98〜-1.00・残り -15dB で最上位に出ることを確かめた（dp/beamenc.c）。本物の ROM では、端数 0〜8bit、差分の単位 / スケール後の単位、4 種の減らし方、4 種の丸め、偏りの位置、係数 m/256、絶対値で計算、端数の持ち越し、2 段の丸めのどれも合わない（最良で残り -0.7dB）。(8) ループの短い持続音（Violin 0/69/40、ループ 72）を 30 秒録って 1 万周平均する方法も試したが、1 周 = 基音 1 周で倍音の線しか無く、フィルタなどの食い違いと分けられなかった（Shanai 0/0/111 はピッチ EG が遅くループの位相がずれて使えない）。(9) 同じ物差しで、さらに: 位置や LFSR で丸めをずらす（ディザ）、負の符号の表の片寄りとの組み合わせ、減らした差分を符号の表の刻み（1,2,4,8...）へ丸め直す形（残りの大きさは合うが相関が無い）、32bit の語の境目だけで丸める形、積算器へ足すときの丸め（端数 1〜10bit）、どれも合わない。持続する音色 56 種 × 3 音を調べたが、モード 0〜2 でループが基音の何周ぶんもあり、単一要素でピッチ EG の速いものは XG の GM 側には無かった（dp/vsweep.txt）。(10) **サンプリングで任意の符号列を実機に鳴らせるか調べた（無理だった）**: カードのサンプルファイル（ALL_SEQ.M2A）は RIFF の RMID / LIST YCVF で、中に `fmt `（WAV と同じ）、`wsmp`、`data`（生の波形）、Yamaha の `v000`（20 バイト）がある。PC で書き換えたファイルを LOAD で読ませて試聴すると、firmware はサンプルの管理情報（CPU の DRAM、例 0x103fa34）の 1 バイトから声の番地のレジスタの上位（形式・スケール・モード）を作る（試聴は 0x13b612 で書く。管理情報から RAM 0x43e973 へ写すのは 0x12a964）。その 1 バイトは `fmt` のビット数だけで決まり、16bit → 0x00、8bit → 0x80（8bit 形式で鳴る）。12/4bit などは「SmplBitSize Err!」、wFormatTag を変えると「Unsupported Fmt!」。`v000` の 8〜15 バイト目は開始位置・ループ・番地に効くが、形式のビットには届かない。firmware のサンプル処理（0x12aa14）には 0xC0（圧縮）の分岐もあるが、そこへ値を入れる入口は LOAD にもパネルにも見つからなかった。道具は scratchpad の dp/smptrace.cpp、smpload.cpp、m2avar.py **2026-09-16: 解決した**（upstream 19 の追記）。galibert さんに mamedev/mame#16143 で尋ねたところ、MAME のコミット b75872cb の議論を教えてもらった。そこで公開されていた SWP00（MU50）と SWP20（MU80）の実機の出力（同じピアノの圧縮サンプルを全形式で鳴らしたもの）に合う形を見つけた: 積算器は漏れず、次の差分は floor((k·Deff + r)/d)、r は余りを負の側に取って次へ持ち越す、Deff は上限で切り詰めた後に実際に足せた量。SWP00 は 7 万サンプル、SWP20 はスケール 1〜7 の 28 形式が各 8 万サンプルすべて一致。素のピッチの実機の録音（m1・m2・n48）で、基音より下の余計な成分が消え（+10〜33dB → 実機の雑音の床）、モード 2 は倍音の位相も一致した。道具は scratchpad の og/（s20clip.py、cpptest.cpp） |
| **ドラムキットと、深い音量 LFO の音色の実機との比べ（2026-09-15 夕方）** | 音ごとに立ち上がりで合わせ、実機が頭から 45dB 以内の間だけで比べる物差し（scratchpad の dp/notecmp.py）に変えた（前の物差しは短い音の MIDI の揺らぎと実機の雑音の底で大きく出ていた）。Standard Kit 71 音で帯の食い違いの平均 0.118dB、ほか 9 キット（Room・Rock・Electro・Analog・Jazz・Brush・Classic・SFX1・SFX2）465 音で 0.272dB、GM 128 音色で 0.169dB。残り: (1) Jazz・Brush・Classic のハイハット（42/44、8bit のサンプル）で実機の尾が -80dB 付近で 3dB ほど早く下がり、早く無音になる。(2) SFX2 の 63/64 は、鳴らすたびに音の高さが乱数で変わる音だった。キットを選び直した直後に 3 回ずつ鳴らすと、実機とエミュで 3 回とも同じ高さの並びになる（1249→2494Hz、195→1736→3461Hz、795→1583Hz など）。キットの比べでずれたのは、乱数の列のどこで鳴らしたかが違ったためで、直すものは無い。Electro・Analog の 28（エミュの 12.8kHz が +24dB）は、逆向きのサンプルの補間の並びが逆だったためで、直した（upstream.md の 35）。(3) Low Bongo（61、12bit）がどのキットでも 5〜8kHz で 4dB 足りない。→ 2026-09-15 夜: 録る経路の話だった。エミュの出力を 15.2dB 下げて 16bit で 0 の側へ切り捨てると（実機の S/PDIF と同じ扱い）、頭 30ms も 0.1〜0.25 秒も全帯域で ±3dB 以内に重なる。足りなく見えたのは、実機の静かな高い帯に乗る切り捨ての雑音の分。(4) Slap Bass 2 は基音側の要素（0 番地の正弦のループ、アタック 0x50・初め 0x7e、減衰 1 は 0x38 で 0x32 へ）が、実機では頭が 3dB 大きく、減衰が遅く 1 秒で -14dB（エミュは 0.3 秒で -18.75dB に着いて平ら）。この要素はフィルタの入力レベル（slot 01 の上 8bit、ff→b6→aa）とカットオフを firmware が時間とともに書き換えるので、エンベロープかフィルタかはまだ分からない。→ 2026-09-15 夜: どちらでもなかった。基音側の要素は遅れて鳴る層（0x011E を書いてから鍵を押す）で、倍音側の要素にも同じ高さの基音がある。GM の並びの中で鳴らしたときは 2 つの基音がたまたま打ち消し合う位相だった。単独で鳴らしたエミュの 2 つの声を足すと実機の基音の減り方に 0.25〜0.77dB で重なる。残るのは下の「遅れて鳴る要素」の約 64 サンプルの件。(5) 深い音量 LFO（Rain・HrmoRain・AfrcnWnd・ToHeaven が型 0・振幅 62、Parasite・Cicada・GblnTalk が型 1）を実機で録ると、揺れの深さと平均の音量はエミュと合う。FX Rain の高い帯の差は -85dB 付近の雑音の底の話だった |
| **GM 128 音色を C2・C6・弱い打鍵で比べた（2026-09-15 夜）** | 送り 0 で C2（音 36）・C6（音 84）・C4 のベロシティ 40 を実機とエミュで録り、dp/notecmp.py で比べた。帯の食い違いの平均は C2 0.173dB、C6 0.366dB、弱い打鍵 0.878dB（実機が小さすぎて比べられた音は 56）。大きく出たものは調べると、ほとんどエミュの間違いではなかった。(1) **尾が実機の方が早く無音になる**（弱い打鍵で 0.1〜0.3 秒）のは、録る経路の話だった。実機の S/PDIF の値はエミュより約 15.2dB 小さく、16bit で 0 の側へ切り捨てられている。エミュの出力を 15.2dB 下げて 16bit で切り捨てると、実機の尾の下がり方と無音になる時刻がほぼ重なる。キットのハイハットの尾が -80dB で 3dB 早く下がる件（下の行の (1)）も、たぶん同じ理由。(2) C6 の Trumpet の 0.5 秒から後と Reed Organ の頭 0.3 秒に、実機だけ白い雑音（音より 17dB 下）が出た。同じ音を CC71・CC74 を変えながら単独で鳴らし直すと、どの組み合わせでも出ないので、その回だけの出来事。(3) 弱い打鍵の 77 番（Blown Bottle）は、実機だけ 0.44 秒遅れて鳴り始めた。1 回だけの遅れで、形は同じ。(4) 同じ音を 3 回ずつ鳴らすと、Syn Strings 1・2 は実機もエミュも毎回同じ形になる。ただし 2 つの要素の音程差（1/1024 オクターブの目盛りで 7）によるうなりの形が、倍音によっては実機と少し違う（音量で 2〜5dB。2 倍音の谷の時刻は 10ms 以内で合う）。原因はまだ分からない。道具は dp/ccsweep.py、subt.py、envt.py、atk.py、beat.py。 **同じ夜の続き（遅れて鳴る要素）**: (4) のうなりの違いは、2 つ目の要素が鳴り始めるまでの遅れが実機とエミュで違うためだった。firmware は要素の遅れを、要素の表の +0x46 の番号から表 0x1E69F0 を引いた数（French Horn は番号 2 → 40）で持つ。2.5ms ごとのメインループ（0x1166C4、400Hz の割り込み 0x309C の直後）で 0x12C598 が 5 ずつ減らし、0 以下になったら鍵を押す（0x12E39C で 0x01xx を書いたチャンネル）。エミュは設計どおりに動いている（40 → 8 回 → 17.5〜20ms、音の時刻によって 2.5ms の幅でばらつく）。実機の遅れは、エミュが鳴らした要素の声（--dump-dac の awm）を録音に最小二乗で当てると誤差 1e-5 で決まる（dp/delfit.py）。French Horn C4 を 0.37ms ずつずらして 24 回鳴らすと、実機は 703〜804 サンプル（平均 752.5）、エミュは 761〜865（平均 815.5）で、幅は同じで、実機が 63 サンプル短い。Music Box（番号から 16 回、エミュ平均 38.78ms）でも実機は 65 サンプル（1.47ms）短く、遅れの長さに比例しない。ピアノと同時に鳴らすと、ピアノと 1 つ目の要素の間はエミュも実機も 52 サンプルで同じ（dp/pair.py、fit3.py）。つまり、普通の鍵を押してから鳴るまでに、実機は前もって 0x01xx を書いたチャンネルより約 64 サンプル（1.45ms）余計にかかっているように見える。SWP30 が鍵を押したときに何か（前の音の消音など）を 64 サンプルかけてやっていて、0x01xx の書き込みでそれを前もって済ませている、という読みだが、確かめる手がまだ無いのでエミュには入れていない。 **→ 2026-09-15 深夜に解決（upstream.md の 36）: SWP30 への書き込みで CPU が次のサンプルの区切りまで待たされるのを入れると、平均も幅も音ごとの値も実機と合う。以下はそこに至るまでの記録。** **CPU の速さを疑った（同じ夜）**: firmware は BSC を BCR1=0x2006・BCR2=0x400C・WCR1=0x2322 に設定する（CS0 のフラッシュに 2 ステートの待ち、CS2 に 3 ステートの待ちと前後の 1 ステート）。実機の SH7043 は 1 命令に 4 クロック前後かかるはずで、エミュの「1 命令 = 1 サイクル」は速すぎるかもしれない。解釈実行で 1 命令あたりのサイクルを 1/2/3/4/6/8 と変えると、French Horn の遅れの平均は 815.5/789/780/769.5/770/790 サンプルで、4 のとき実機（752.5）に 17 サンプルまで寄る（幅は 95 で実機の 101 と同じ。6 から先は 1 回の処理が 2.5ms を越えて幅が崩れる）。ところが同じ 4 で SLICE の刻みの位相は 34 → 48ms と実機（平均 9ms）から遠ざかり、起動も 7.9 → 8.7 秒と少し延びるだけ。1 つの速さでは両方を説明できないので戻した（バスの区画ごとに待ちを数える形なら違うかもしれない）。 **起動の動画で CPU の速さを確かめた（同じ夜、オーナーが実機の電源投入を 30fps で撮影）**: 画面の切り替わりの間隔を、エミュ（gui の NVRAM を読ませて起動）と比べた。タイマーで待っているらしい区間は合う: HostSelect→Checking PLG は実機 1.03 秒 / エミュ 1.00 秒、Checking PLG→通常画面は 1.57 / 1.55 秒（1 命令 4 サイクルにすると 1.66 秒で外れる）。合わないのは画面を動かしている区間で、Welcome の流れ込みが 0.54 / 0.35 秒、流れ終わり→MIDI IN-A=rear が 2.03 / 1.44 秒、→HostSelect が 0.73 / 0.55 秒と、実機が 1.3〜1.5 倍長い（ただし実機は HostSelect=USB、エミュは MIDI）。流れ込みの 1 歩はエミュで 50ms、1 命令 4 サイクルでも 50ms のままなので CPU では決まっていない。CPU が一律に 4 倍遅いという読みは、Checking PLG の区間と合わない。流れ込みの速さの違いは LCD の周り（HD44780 のビジーの長さなど）を見る余地がある |
| **XG の GM 128 音色の実機との比べ（2026-09-15）** | 送り 0・C4・1.5 秒で 128 音色を実機で録って比べた（scratchpad の dp/gm.mid、gmcmp.py）。**声の音量を掛けた結果の端数**を 256 の刻みで切り捨てるようにして（upstream.md の 34）、帯の食い違いの平均が 0.560 → 0.349dB。残りで目立つもの: (1) Slap Bass 2（GM 38）の基音側の要素が、実機はなだらかに 1 秒で -15dB 下がるのに、エミュは減衰 1（速さ 0x38、目標 0x32）で 0.2 秒で -11dB 下がって平らになる（上の帯は 0.0dB で合う）。同じ速さの減衰がほかの音では実機の方が速い向きに外れていて、単純な速さの表の違いではない。(2) Choir Aahs（GM 53）は約 1 秒周期の音量の揺れの位相が違う（声ごとの LFO の始まり）。(3) Taiko・Bird Tweet・Seashore・FX Rain の食い違いは、音の短さ（MIDI の揺らぎで窓がずれる）と雑音の音色のため。(4) 減衰の速い音で、エンベロープの速さそのものは実機とおおむね合う（サンプル自体が減衰しない音で比 1.00〜1.05） |
| **`sin-table.bin` が自作の近似** | `tools/dump/make_standins.py` で 1/4 波 sin を合成している（実チップの表は CRC が合わず、何か加工してある）。2026-09-15: **前の代替品は 0 から 65535 へ上がる形で、MEG の LFO が後ろ半周期を ^0xffff で作ると山と谷の境目で値が跳んでいた**。中心 0x8000 から 0xffff へ上がる形に直した（前の形のファイルは `mu2000::load_sintab` が読むときに作り直す）。実機との揺れの食い違いは CHORUS 3 8.47 → 0.11、CELESTE 1-4 5.2〜8.1 → 0.2〜0.5、SYMPHONIC 1.06 → 0.12dB（fx/sys.mid の 18 種類で合計 38.1 → 5.9）。バリエーションの CHORUS 3 も合った。前に「位相の欄に 0 と 8 以外を使う種類だけ合わない」と見えていたのはこれが原因で、位相の表は MAME のままでよかった。パフォーマンスモードの 100 パフォーマンスの帯の平均は 0.871 → 0.895dB（032 Alien Peace 4.9 → 1.9、095 Door Bell 1.2 → 0.2、087 Float Pad 1.6 → 0.6 が良くなり、042 AlmostHeaven 1.3 → 5.1、014 Fancy Pad 1.3 → 3.5、069 Guitar& 0.8 → 2.5 が悪くなった。042 は CHORUS 1、014 は FLANGER 1、069 は CELESTE 1 を使う。1 回ずつの録音で LFO の位相に敏感な物差しなので、揺れを直接比べる sys.mid を正とした）。残りの CHORUS 1 の 2.36dB は測り方のせい: 起動したときのコーラスが CHORUS 1 なので、sys.mid で選び直しても firmware は LFO を書かず（エミュで確かめた）、揺れの位相が電源を入れてからの時間で決まる。実機とエミュで起動の時刻が違うので比べられない（同じ LFO の値の FB CHORUS は 0.18 で合う） |
| **LCD のフォントが実機と一致しない** | 代用品は Adafruit の 5x7 から起こしたもので、0x80 以上は全部空白にしてから推測した 4 字とレベルメータだけ描いている（`tools/dump/make_standins.py:36`）。記号が合わない。実機から撮るなら、全 256 コードを DDRAM へ書かせる小さなダンパを流して LCD を撮る手がある |
| MEG とレジスタ間の通信タイミングが未実装 | MAME の TODO（`src/mame/sound/swp30.cpp:21`）。鳴っている最中にエフェクトのパラメータを変えたときの挙動がずれる可能性。エディタ画面から触る用途で出る |
| MEG 命令の 7bit・LFO slot 9・slot b が未解明 | 同じ TODO。どの効果で出るかも不明 |
| 出力が 16bit 固定で、0 方向の整数除算 | `l * 32768 / DAC_FULL_SCALE`（`src/render.cpp:142` 他）。比較の基準にする波形が 16bit に丸められている。直流を追うなら float で出せたほうがいい |

#### ピッチ EG の残り（issue #3 の DX Hard などは直した。doc/upstream.md の 13）

SWP30 の中のピッチ EG（スロット 0x0B・0x10、内部ポート 4）を足して、DX Hard・CheezOrg・SloRotar・
GlaclRtr・RotaryOr の音程が実機と合った。残っている分からないこと:

* 速さは音量の EG の表を 16 段遅らせて引いている。16 より小さい速さも符号付きのまま引いて伸ばす（XG の SFX Starship が速さ 8。swp30.cpp の peg_step）。2026-09-17 に、実機と差の出た 116 音色をずらし方 8 / 12 / 16 / 20 / 24 で書き直して実機の 2 回録りと比べると、合計は 370 / 346 / **325** / 340 / 355 で 16 が一番近い
* スロット 0x10 の bit 14 が何の印なのかは未確認（足すかどうかには関係しなかった。upstream 13 の追記）
* 着いた印を firmware が読む間隔（10ms おき）と実機の位相はずれうる。重ねた層がうなる音色
  （055 PowerSynthBR・076 Orion・090 Pad Swell）は、実機でも弾くたびに 2〜3dB 揺れるので、1 回の比べでは決められない

### 実時間性・性能

| | |
|---|---|
| 小ブロックでの取りこぼしが未実測 | `m_late` を数えてはいる（`src/vst3/plugin.cpp:679`）が、何サンプル以下で落ちるかの数字が無い。128/256/512 で測る。落ちるなら「1 ブロック先に作って申告する」安全側の選択肢も要る |
| ~~複数挿しでスピンスレッドが増える~~ 済 | 別スレッドにするのは、動いている台数が論理コア数の 1/4 以下のときだけにした（`SMU2000_THREADED_MAX`）。台数が変わると run_sample の中で切り替える（途中で 4 回切り替えても出力は 1 本のときと一致）。16 論理コアで dense を並べて回すと、4 台なら別スレッドが速い（2.55 秒）、16 台は全部 1 本で 5.14 秒（前は全部別スレッドで 10.89 秒） |

### 機能の穴

| | |
|---|---|
| **A/D 入力とサンプリング（途中）** | 2026-09-15: サンプリングの録音と試聴が通るようにした（`build/samptest.exe`、make test の 6 番）。分かったこと: SAMPLING の品書きは EDIT/LOAD/SAVE/REC/UTIL/RAM。REC で Enter を押すと、firmware はスレーブの SWP30 に番地 0x1000000（サンプリング RAM の先頭）と長さ 0x100000 語を書き、波形アクセスに 0x7000 を書く。あとは 0x30f（書いた 16bit サンプル数の下 16bit）と 0x10f の bit 14（書き終わり）を読み続け、止めるときアクセスを 0 に戻す。データは 16bit モノラル 44.1kHz、32bit の語に 2 つずつ（声が 16bit のサンプルを読む並びと同じ）。サンプリング RAM は 4MB を SWP30 の 0x1000000 語目に置き、2 チップで共有する（`flat_space::set_overlay`）。入口は `mu2000::set_audio_input`（左 AD1、右 AD2）、`render --adc-in` で WAV を流せる。残り: (1) REC の InputSrc（AD1/AD2/AD1+2）は入れた（2026-09-15）: REC の画面に入ったときと値を変えたとき、firmware（0x13af14）がスレーブの MELI 6/7 のミキサの音量（0x5b8/0x5b9、0x5f8/0x5f9）を 0x00ff（通す）か 0xffff（止める）にする。2 本はミキサの出力 8 の左にだけ繋がっているので、録音はそこを録る（`m_rec_bus`）。見落としていたのは、REC の画面に入るときにだけ書いていたから。InputGain（line/mic）は 0xd80000 の留め金（RAM の写し 0x43e8ae、bit 3-4 の 2bit）らしいがまだ解いていない（アナログの増幅なので、エミュでは入力の大きさを変えるだけになるはず）。(2 の続き) レベルメーターと TriggerLvl は入れた（2026-09-15）: firmware は CPU の A/D 変換器の AN0（AD1）と AN2（AD2）を回し続けて読み、0xff から引いて使う（静かなほど値が大きい。引いた値 0x18 でメーター 0、0x85 で振り切れ）。A/D 変換器の連続変換がエミュで動いていなかった（upstream.md の 28）。検波の回路は分からないので、ピーク（0.1 秒で 1/e に下がる）を 0x18〜0x85 に割り当てた。実機のメーターの振れ方との突き合わせはまだ。(2) きっかけ（Waiting!）とレベルメーターは入れた（下の (1) の後ろ）。(3) A/D パートは入れた（2026-09-15）: A/D INPUT はスレーブの MELI 6（AD1）と 7（AD2）に入る。エミュで空いている線に 1 本ずつ試験の正弦を入れ、A/D パートの音量（10 00 0B、初期値 0）を上げたときに鳴る線で決めた。目盛りは 16bit を 8bit 上げた 24bit（音量 100 でほぼ入力と同じ大きさ）。実機の入力の大きさとの突き合わせはまだ。(4) gui の音声入力（A/D INPUT のジャックで録音デバイスを選ぶ、`ui::audio_in`）と VST3 の補助の入力バス `A/D Input` は入れた（2026-09-15）。(5) SmartMedia は入れた（2026-09-15、下の行）。 |
| SmartMedia（途中） | 2026-09-15: 入れた（`src/smartmedia.h`）。制御の留め金 0xD00000（bit 5 電源、bit 1 選ばない、bit 0 選ぶ、bit 3 CLE、bit 2 ALE）とデータの口 0xC00000 で NAND の命令をそのまま送る。ポート A の PA18 が忙しい（0 で準備よし）、PA19 が差し込み、PA20 が書き込みを禁じていない。名乗りは東芝（0x98）の 16/32/64/128MB。firmware は SSFDC の物理の書式（先頭のブロックの CIS、予備の領域のブロックの番地、ECC）を確かめるので、新しいカードには CIS だけ入れておき、論理の書式は UTIL → CARD → Format に書かせる。SAVE のサンプルの書き出しに SWP30 の続けて読む働き（波形アクセス 0x9000、upstream.md の 27）が要った。samptest で書式化 → SAVE ALL+SEQ → 別の機械で LOAD、サンプリング RAM が一致。gui はカードの差し込み口の品書きで作る・差す・抜く（書き換えたブロックを 2 秒ごとに書き戻す）、render は `--card`。残り: (1) VST3 にもカードを入れた（2026-09-15。差し込み口の品書き、プロジェクトにはファイルの場所だけを覚える。同じファイルを複数で差すのは防いでいない）。(2) PC から中のファイルを出し入れする道具は入れた（`tools/smcard.py`、2026-09-15。16/32MB は FAT12、64/128MB は FAT16。32MB と 128MB で、入れた WAV を本体の LOAD で読み、本体が SAVE した WAV を取り出して確かめた。2 つめ以降のゾーンに書く場合はまだ試していない）。(3) 実機のカードでの突き合わせ（ECC の並び、ブロックの割り当て）。(4) 状態の保存にはカードの中身を入れていない（FSM だけ）。 |
| USB (M37640) 代役だけ | 0xF80000。2026-09-16: **MIDI IN の口 C・D が通るようになった**（doc/dump/usb.md「MIDI IN C・D の口」）。SH-2 から見えるのは 2 番地と割り込み 2 本だけなので、そこに代役を置き、`F5 <口>` を挟んだ MIDI バイト列を渡す。HOST SELECT を USB にする必要があるので `mu2000::set_usb_host()`、gui は `--usb` か口 C・D の指定で自動。残り: (1) 一覧の窓と xg の写しがまだ 32 パートぶんで、33-64 は見えない。(2) USB の口 5-8（ファームが捨てている）と、出力の 8 本の使い分け。(3) ホストモード（MU2000 に MIDI 機器を繋ぐ）は M37640 の中身が要るので今も無理。IC5 の ROM も未吸い出し |
| 未マップ番地の読みが黙って 0 | `src/compat/membus.h:64`。0xD80000（コントラスト・レベル）や 0xE80000（デジタル出力）も含めて何も言わずに 0 が返る。記録する口が無いと「実は触っていた」に気づけない |

### 配布と体制

| | |
|---|---|
| CI が無い | `.github` が無い。ROM 無しでもビルドは通るので、それだけでも回す価値がある |
| Windows 専用 | `windows.h` を使うファイルが 19 本。音声・MIDI・窓・ファイル経路が全部 Win32。macOS/Linux と CLAP/AU をやるなら、早めに層を切ったほうが安い |
| 配布物が無い | 利用者に MSYS2 を建てさせている。ROM は同梱できないが exe は配れる |

### 細かいもの

* 1 ブロックの MIDI が 8192 件を超えると黙って捨てる（`src/vst3/plugin.cpp:513`）。
  まず起きないが、起きたときに分からない
* `flush_resampler` を誰も呼んでいない（`set_output_rate` の中だけ）
* LCD の行 0 桁 19 が未解明（doc/gui.md）、セグメントの表に不明 2 件
  （doc/lcd-segments.md）
